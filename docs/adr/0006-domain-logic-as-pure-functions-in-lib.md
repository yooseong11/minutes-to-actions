# 0006. 도메인 로직은 lib/의 순수 함수로 분리한다

## 결정
날짜·대조·후처리·내보내기 로직은 `lib/`에 순수 함수로 두고, API는 요청 검증과 모델 호출, 응답만 담당한다.

## 이유
LLM 호출 없이 단위 테스트를 돌릴 수 있어 검증이 빠르고 API 비용이 들지 않는다.

## 버린 대안
- API 핸들러 안에 로직: 테스트마다 실제 모델 호출과 비용이 든다.
- 프론트에서 후처리: 같은 로직을 서버와 클라이언트에 두 번 쓰게 된다.
