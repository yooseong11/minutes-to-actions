# 0005. 모든 항목은 원문 인용을 제출한다

## 결정
모든 항목은 `quote`(번복 시 `supersededQuote`)를 함께 제출하고, 정규화한 원문에 그 문자열이 없으면 `rejected`로 분리한다.

## 이유
문자열 대조는 코드가 확실히 할 수 있는 환각 탐지라서, 지어낸 항목이 결과에 섞이지 않는다.

## 버린 대안
- 인용 없이 내용만 제출: 환각을 기계적으로 가려낼 방법이 없다.
- 유사도 비교로 통과 판정: 임계값을 넘는 지어낸 문장이 그대로 통과한다.
