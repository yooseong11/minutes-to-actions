# 0013. 프론트는 동일 출처 API만 호출한다

## 결정
프론트는 동일 출처 `/api/extract`만 호출하고 서버에 CORS를 열지 않는다. 개발 중에는 Vite가 배포 API로 프록시한다.

## 이유
CORS를 열면 다른 사이트가 이 서버의 API 키 비용을 쓸 수 있다.

## 버린 대안
- CORS 전체 허용: 외부 사이트가 API 키 비용을 소모한다.
- 프론트에서 OpenAI 직접 호출: 키가 브라우저에 노출된다(0002).
