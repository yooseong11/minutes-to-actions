# 0011. 항목 ID는 결정적으로 생성한다

## 결정
항목 ID는 같은 입력이면 같은 값이 나오도록 항목 내용에서 결정적으로 만든다.

## 이유
저장소가 없어도(0001) 편집·되돌리기·회귀 대조에서 같은 항목을 안정적으로 가리킬 수 있다.

## 버린 대안
- 랜덤 UUID: 다시 추출하면 ID가 바뀌어 회귀 대조와 되돌리기가 깨진다.
- 배열 인덱스: 항목 하나를 지우면 뒤 항목의 식별자가 전부 밀린다.
