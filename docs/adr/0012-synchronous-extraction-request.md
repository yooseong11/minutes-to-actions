# 0012. 추출은 동기 요청 하나로 처리한다

## 결정
브라우저가 `/api/extract` 결과를 한 번에 기다리고, 작업 큐·작업 상태 API·폴링을 두지 않는다.

## 이유
저장소가 없는 1차 범위(0001)에서 구현과 설명이 가장 단순하다. 대신 모델 응답이 시간 제한을 넘으면 504가 난다.

## 버린 대안
- 작업 큐 + 폴링: 504를 구조적으로 없애지만 DB와 상태 API가 필요해 1차 범위를 넘는다.
- 스트리밍 응답: 후처리가 전체 JSON을 필요로 해 중간 출력이 쓸모가 없다.
