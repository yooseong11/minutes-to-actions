# 0016. 내보내기는 마크다운·CSV 두 갈래이고 변환은 코드가 한다

## 결정
회의록 본문은 마크다운, 액션아이템은 CSV로 내보내고, 변환은 AI가 아니라 `lib/`의 코드가 한다.

## 이유
마크다운은 노션·슬랙 복붙, CSV는 스프레드시트 필터라는 서로 다른 사용 순간을 덮고, 양식 변경이 코드 수정으로 끝난다.

## 버린 대안
- AI가 마크다운 문장까지 작성: 양식이 호출마다 달라지고 검증할 수 없다(0004).
- 한 형식만 지원: 회의 직후 공유와 몇 주치 모아보기 중 하나가 빠진다.
