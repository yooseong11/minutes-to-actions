# 0014. 모델 설정은 환경변수로 두고 기본값은 코드에 둔다

## 결정
`OPENAI_MODEL`·`OPENAI_REASONING_EFFORT`는 환경변수로 두고, 값이 없으면 코드의 안전한 기본값으로 동작한다.

## 이유
모델 실험을 코드 수정 없이 배포 설정만으로 되돌릴 수 있고, 환경변수가 빠져도 앱이 멈추지 않는다.

## 버린 대안
- 코드에 모델 하드코딩: 모델을 바꿀 때마다 커밋과 배포가 필요하다.
- 기본값 없이 환경변수 필수: 변수 하나가 빠지면 배포가 통째로 죽는다.
