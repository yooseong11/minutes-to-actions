# 0001. 추출 결과를 서버에 저장하지 않는다

## 결정
DB·로그인·회의 목록을 두지 않고, 추출 결과는 브라우저 세션에만 남긴다.

## 이유
회의록은 사내 기밀이라 저장하는 순간 보안 요구와 개발 비용이 함께 늘고, 1차 목표는 추출·검토 흐름의 유효성 증명이다.

## 버린 대안
- DB에 회의 기록 누적: 인증과 보안 설계가 따라붙어 1차 범위를 넘는다.
- 서버 로그에 원문 보관: "남는 데이터가 없다"는 제품 약속이 깨진다.
