# 0002. LLM 호출은 api/extract.ts 한 곳에서만 한다

## 결정
OpenAI 호출은 `api/extract.ts` 하나에만 두고, 다른 코드는 LLM을 직접 부르지 않는다.

## 이유
Azure OpenAI나 Ollama로 바꿀 때 고칠 자리가 한 곳이면 교체 비용이 작다.

## 버린 대안
- 기능마다 호출 추가: 모델을 바꿀 때 호출부를 전부 찾아 고쳐야 한다.
- 프론트에서 직접 호출: API 키가 브라우저에 노출된다.
