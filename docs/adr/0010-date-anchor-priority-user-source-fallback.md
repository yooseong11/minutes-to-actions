# 0010. 기준일 우선순위는 사용자 지정 > 원문 > fallback

## 결정
상대·역산 날짜의 기준일은 사용자 지정, 원문 `meetingDateRaw`, fallback 순으로 고른다.

## 이유
사용자가 고친 기준일을 원문이 덮으면 고쳐도 반영되지 않고, 원문에 날짜가 있는데 오늘로 계산하면 조용히 틀린다.

## 버린 대안
- 항상 오늘 기준: 지난 회의록의 "이번 주 안에"가 실행일마다 다르게 환산된다.
- 원문 우선: 사용자가 기준일을 지정해도 반영되지 않는다.
