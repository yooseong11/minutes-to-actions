# 회귀 기록

더미 회의록 5건(`01`~`05`)을 배포된 `/api/extract`에 넣고,
`00_함정_정답지.md`의 기대 동작과 대조한 기록입니다.

- 측정 대상: 배포본 (로컬 아님)
- 채점 단위: 정답지의 "함정" 한 줄 = 1점
- 만점 31 (01: 8 / 02: 6 / 03: 5 / 04: 6 / 05: 6)

> LLM 출력은 실행마다 흔들립니다. 아래 점수는 **1회 측정값**이며,
> ±1~2점은 개선/악화가 아니라 편차로 봐야 합니다.
> 항목 누락(추출 자체가 안 되는 것)이 특히 흔들립니다.

---

## 점수 추이

| 회차 | 01 | 02 | 03 | 04 | 05 | 합계 | 무엇을 바꿨나 |
|---|---|---|---|---|---|---|---|
| 0차 (수동, API 이전) | — | — | — | — | — | 28/29 | 섹션 1에서 손으로 확인. 스키마 2차 확정 시점 |
| 1차 | 5/8 | 3/6 | 2/5 | 5/6 | 3/6 | **18/31** | LLM 최초 연결 (`gpt-6-astra`) |
| 2차 | 6/8 | 3/6 | 4/5 | 5/6 | 5/6 | **23/31** | 날짜 파서 결함 2건 수정 + 프롬프트 개편 |
| 3차 | 7/8 | 3/6 | 4/5 | 3/6 | 5/6 | **22/31** | AI 판정을 코드로 이동, 프롬프트 축소 |

0차와 1차의 낙차가 큰 이유: 0차는 사람이 한 건씩 보며 채점했고,
1차부터는 API 응답 전체를 기계적으로 대조했습니다. 같은 척도가 아닙니다.

---

## 회차별 변경 내용

### 1차 → 2차

**코드** (`lib/date.ts`)
- `"이번 주 안에"`, `"다음 주까지"` 환산 추가 (그 주 일요일)
- 수식어 없는 요일이 지나갔으면 다음 주로 밂
  - `"화요일 오전"` (회의일 9/11 금) → 9/8 이었음 → **9/15**
  - `"이번 주 X요일"`은 기한이 아니라 지목이므로 과거여도 유지

**프롬프트** (`lib/prompt.ts`)
- 작업 순서를 4단계로 명시하고, 제외 판정을 항목화 **이전**으로 옮김
- 참석자 규칙 신설 — `nameRaw`에 소속 금지, 1인칭·제3자 제외
- 검토 사유 10종에 원문 예시 추가

결과: 03(2→4), 05(3→5)가 크게 올랐습니다.
**부작용 2건** — 01의 대조군(영수증→정다은)이 사라졌고,
02에서 `due_unparseable` 오탐이 생겼습니다.

### 2차 → 3차

남은 실패 5건이 전부 "AI에게 부탁하던 것"이어서, 부탁을 **검사**로 바꿨습니다.

**코드로 이동** (`lib/people.ts`, `lib/postprocess.ts`)
- `assigneeInQuote` — 담당자 이름이 `quote`에 없으면 AI가 다른 발화에서 끌어온 것
- `isFirstPerson` — 나/내가/저/제가 → `assignee_unknown`
- `hasUnclearAmount` — 숫자+천/백/만에 "원"이 없으면 `unit_unclear`
- 번복 접기 — 다른 항목의 `supersededQuote`와 같은 `quote`는 중복이므로 버림
- `CODE_OWNED` — AI가 붙인 사유 8종을 버리고 코드가 다시 판정

**프롬프트에서 제거**
- 검토 사유 10개 중 8개 삭제. AI가 판단하는 것은 `conditional`, `ambiguous_intent` 둘뿐
- `"단순 현황 보고"` 제외 되돌림 (01 대조군을 죽인 원인)
- `SYSTEM_PROMPT` 3,683자 → **2,828자** (개편 전 원본보다도 짧음)

결과: 합계는 23 → 22로 오히려 1점 내려갔습니다.
다만 **고친 것과 깨진 것이 다릅니다** — 아래 참조.

---

## 3차 시점: 고쳐진 것

| 항목 | 이전 | 지금 |
|---|---|---|
| 01 대조군 (영수증 → 정다은) | 사라짐 | **부활**, `reviewReasons: []` |
| 03 1인칭 (`"양식 만드는 건 내가"`) | `no_assignee` | **`assignee_unknown`** |
| 02 `"이번 달 안에"` 오탐 | `due_unparseable` 붙음 | **제거됨**, 9/30 |
| 02 참석자 동명이인 | `"개발팀 김민수"` 로 오염 | `김민수|회계`, `김민수|개발팀` **분리** |
| 05 조건부 결정 | 사유 없음 | **`conditional`** (별도 항목으로 분리까지) |
| 05 애매한 발화 (우산꽂이) | `no_assignee` | **`ambiguous_intent`** |

## 3차 시점: 남은 실패

| # | 샘플 | 기대 | 실제 | 원인 추정 |
|---|---|---|---|---|
| 1 | 01 | 정수기 "견적 받기" → `action` + `no_assignee` | 항목 자체가 안 나옴 | 괄호·부수 발화를 항목으로 안 봄 |
| 2 | 05 | 토너 과다 사용 "확인 필요" → `action` + `no_assignee` | 항목 자체가 안 나옴 | 위와 같음 |
| 3 | 02 | 번복 후 최종 `type: "open"` | `type: "decision"` 유지 | AI가 최종 상태를 안 바꿈 |
| 4 | 02 | `"3천"` → `unit_unclear` | 견적 비교 항목이 안 나옴 | 항목 누락이라 코드 판정까지 못 감 |
| 5 | 04 | 자발적 담당자 이수현 정상 추출 | `no_assignee` 오탐 | `assigneeInQuote`가 너무 빡빡함 |
| 6 | 03/04 | 임원보고·워크샵 항목 | 실행에 따라 누락 | 편차 |

**3차에서 새로 깨진 것은 5번 하나입니다.**
`assigneeInQuote`는 04의 2턴 지시(#6, 의도적 설계)를 막으려고 넣었는데,
같은 전사본의 자발적 담당자("제가 재무팀 이번 주에 볼 일 있어서")까지 걸렸습니다.
`quote`에 화자 라벨이 포함되지 않으면 이름을 못 찾습니다.

---

## 읽는 법

점수보다 **어디서 틀리는지**가 중요합니다. 세 회차를 통틀어:

- **코드가 맡은 일**(날짜 환산, 역산, 참석자 대조, 환각 탐지)은 한 번도 틀리지 않았습니다.
  환각(`rejected`)은 5개 샘플 × 3회 = 15회 실행에서 **0건**입니다.
- 틀린 것은 전부 **AI가 판단하는 칸**과 **무엇을 항목으로 볼 것인가**입니다.
- 프롬프트로 고쳐지는 것(지시가 불명확했던 것)과 안 고쳐지는 것(모델이 사람처럼
  추론하려는 것)이 갈립니다. 후자는 프롬프트를 조여도 잘 안 됩니다.

## 다음에 손댈 때

1. **#5부터.** 새로 깨진 것이므로 우선순위가 가장 높습니다.
   `assigneeInQuote`에 화자 라벨(`[14:28] 이수현:`)을 근거로 인정하는 경로 추가.
2. #1, #2, #4는 같은 원인(항목 누락)입니다. 하나를 고치면 셋이 같이 움직일 수 있습니다.
3. #3은 프롬프트로 두 번 실패했습니다. 코드로 옮기는 쪽을 검토 —
   `supersededQuote`가 있으면 `type`을 `open`으로 강제하면 됩니다.
4. 모델을 바꿔볼 거면 **한 번에 하나만.** 프롬프트와 같이 바꾸면 원인을 못 가립니다.
