# 회의록 추출기 — 인수인계 문서

> 새 대화를 시작할 때 이 파일을 먼저 올리세요.
> 최종 갱신: **2026-09-15 (7차 갱신)**
>
> **섹션 5(내보내기)가 끝났습니다.** 마크다운은 클립보드 복사, CSV는 파일
> 내려받기입니다. 둘 다 `lib/`의 순수 함수이고 `npm test` **100 passing** ·
> `typecheck` · `build` 통과입니다. 마크다운은 화면에서 눈으로 확인했습니다.
> 남은 것은 섹션 6 README와, 같은 설정으로 504가 없는지 재현 확인하는 일뿐입니다.
>
> **504 응답 시간 초과는 `gpt-5.6-sol` + reasoning `medium`으로 해결했습니다.**
> 2026-09-15 회귀 실행에서 5건 모두 22.4~40.2초 안에 돌아왔습니다.
> 회귀 6차 손채점은 **38/41(92.7%)**, 핵심 안건 평가는 **10/10**입니다.
> 다음은 같은 설정으로 504가 없는지 한 번 더 재현 확인하는 일입니다.
>
> **섹션 4.5(항목 단위를 안건으로)는 끝났고 배포까지 했습니다.** 화면도 눈으로
> 확인했고, 카드를 걷어내 항목을 한 줄로 바꿨습니다(아래 「해결됨 — 화면」).
> `npm test`는 섹션 5까지 넣어 현재 100 passing입니다.
>
> **용어가 정리됐습니다.** 안건 = 화제 하나(agenda), 항목 = 그 안의 결정·할 일·미결
> (item). 이전 화면이 항목을 "안건"이라 부르던 곳은 전부 "항목"으로 바꿨습니다.
>
> 이전 상태: 섹션 4(편집) 완료 — 담당자·기한·항목 수정/삭제, 항목 추가, 분류 변경,
> 되돌리기. 사용법은 [편집 안내](docs/editing.md).
> 회귀 6차는 **5건 모두 응답 완료, 38/41**입니다. 새로 추가한 안건 10점은
> 전부 맞았고, 기존 항목은 28/31로 5차보다 1점 낮습니다 (`docs/regression.md`).

## 배경

- 경영지원팀 **AI 업무자동화 엔지니어** 직무 지원용 포트폴리오
- JD 키워드: 회의록 정리·보고서 자동화, 사내 챗봇, LLM API 활용, 업무 프로세스 분석을 통한 **자동화 과제 발굴**
- 기존 포트폴리오: 타임블록 (React 19 + Vite + Supabase + Vercel, 자연어 → 일정 구조화)
  - 재사용한 것: `api/parse-task.js` 구조, Responses API + Structured Outputs, 상태코드별 한국어 에러 문구
- 목표 기간: 원래 1일(45분 × 5섹션). 현재 **섹션 4.5까지 완료**

## 확정된 제품 판단

| 판단 | 이유 |
|---|---|
| **텍스트 입력 전용. 녹음 안 함** | 클로바노트·다글로·티로는 음성 처리 도구. 한국어 STT로 경쟁하면 짐. 텍스트는 겹치지 않음 |
| **공식 지원 범위는 한국어 회의록 700자까지** | 포트폴리오용 1차 제품이고, 동기 LLM 호출의 시간·토큰 비용을 감당할 수 있는 검증 범위만 약속함. 700자를 넘는 입력이 동작할 수는 있지만 품질·응답 완료를 보장하지 않음 |
| **웹앱으로 만듦 (스크립트 아님)** | 쓸 사람이 비개발자. 스크립트면 자동화가 아니라 대신 돌려주는 일 |
| **HWP 안 함** | 사내 회의록은 HWP를 안 씀. HWP는 대외·공공 제출용 |
| **출력 2갈래** | 회의록 본문 → 마크다운(노션 복붙) / 액션아이템 → CSV(엑셀 필터) |
| **AI는 발췌만, 계산은 코드** | 날짜·이름을 AI가 변환하면 틀려도 티가 안 남 |
| **quote 필수** | 원문 인용을 코드로 문자열 검사 → 환각 탐지 |
| **`type` 3종 유지** (decision/action/open) | 샘플 02의 번복된 결정이 갈 곳이 open뿐 |
| **검토 사유는 enum** | 자유 문장이면 사람마다 결과 양식이 달라짐. 코드가 UX 분기를 못 함 |
| **추론해서 채우지 않음** | 여러 발화에 걸친 담당자는 비우고 되묻기 |
| **DB 저장 안 함 (1차)** | 먼저 간단히 완성해서 실물을 보고 판단 |
| **로직은 `lib/`로 분리** | 순수 함수로 만들어 LLM 없이 단위 테스트 |
| **TypeScript** | `ReviewReason` 10종 등 enum을 컴파일 단계에서 잡기 위함 |
| **AI가 판단하는 칸은 6개** (4.5에서 4개→6개) | `type`, `content`, `conditional`, `ambiguous_intent`, **`title`(안건 제목), `summary`(안건 요약)**. 나머지 사유 8종은 코드가 판정 |
| **기준일 출처 3개, 우선순위 고정** | 사용자 지정 > 원문 > 오늘. (B)안으로 확정 — 아래 참조 |
| **결과 화면은 회의록 문서 형태** | 항목 목록이 아니라 **회의록 5대 필수 요소** 순서. 사람이 회의록을 읽는 순서가 따로 있음 |
| **안건 제목·요약만 AI의 문장 작성을 허용** | 단 "확인이 필요합니다"를 화면에 명시. 검증 안 된 칸이라는 걸 감추지 않음. 회의 전체 요약은 안건별 요약과 중복돼 제거함 |
| **"요약하지 않습니다"를 지우지 않고 좁혔음** | 원칙을 통째로 없애면 AI가 다른 칸에서도 문장을 쓰기 시작함. 예외는 안건 `title`·`summary` 두 칸뿐 |
| **회귀 측정 `fallbackDate`는 2026-09-14 고정** | 오늘 날짜로 재면 "이번 주 안에"가 실행일마다 다르게 환산돼 회차끼리 비교가 안 됨 |
| **회귀 채점은 사람이 함** | 스크립트는 응답만 남김. "무엇을 항목으로 볼 것인가"가 제일 자주 틀리는데 그건 기계가 못 셈 |
| **읽기 단위는 안건, 검증 단위는 발화 항목** (2026-09-14 확정) | 한 발화 = 한 항목은 환각 탐지를 위한 단위이고, 사람이 회의를 읽는 단위는 안건. 발화 항목을 안건 아래 묶어 두 요구를 함께 만족시킴 |
| **단위를 바꾸지 않고 위에 한 겹을 올림** | 항목은 여전히 발화 단위이고 quote 대조·CSV 내보내기도 그대로. 달라진 건 항목이 안건 **안에** 들어간다는 것뿐. 단위를 바꿨으면 환각 탐지를 다시 만들어야 했음 |
| **안건 제목은 AI가 씀** (2026-09-14 확정) | 첫 발화를 그대로 제목으로 쓰면 검증 칸이 하나만 늘지만, 제목이 "자 시작하죠. 오늘은 사내 문의 대응 건인데요"가 됨. 읽으라고 만든 칸이 안 읽힘 |
| **회의 전체 요약(`discussionSummary`) 제거** (2026-09-14 확정) | 안건별 요약과 같은 내용이 화면에 두 번 나옴. 전체 요약이 "정수기 얘기가 나왔다"를 쓰고 정수기 안건 요약이 또 씀 |
| **안건은 항목 id 목록을 갖지 않음** | 소속은 항목 쪽 `agendaId` **한 군데에만**. 양쪽에 적으면 사용자가 항목을 지웠을 때 한쪽이 반드시 어긋나고, 화면이 없는 항목을 그림 |
| **넓은 화면도 한 열** | 칸반 3열을 걷어낸 자리에 2열 그리드를 넣지 않았음. 안건은 위에서 아래로 읽는 순서가 있는데, 두 열로 쪼개면 그 순서가 사라짐 |

## 아키텍처 결정 모음 — 현재 설계의 단일 요약

아래 표가 현재 아키텍처를 빠르게 파악하는 기준입니다. 뒤쪽의 긴 섹션은 결정 당시의
실험·실패·근거를 보존한 기록입니다. 둘이 다르면 이 표와 현재 코드를 먼저 봅니다.

### 1. 제품 범위와 비용 경계

| 결정 | 현재 상태 | 이유와 대가 |
|---|---|---|
| 입력은 붙여넣은 한국어 회의록 텍스트 | 음성·파일 업로드·STT 없음 | 기존 음성 회의록 제품과 경쟁하지 않고 업무 자동화의 구조화 문제에 집중 |
| **공식 검증·지원 범위는 700자까지** | 사용자에게 약속하는 범위를 700자로 제한. 회귀에는 공백 포함 765자인 04번 스트레스 샘플도 있음 | 포트폴리오 제품에서 감당 가능한 API 비용과 대기 시간을 명시적으로 제한. 더 긴 입력은 동작할 수 있어도 보장하지 않음 |
| UI 입력은 700자로 제한, API 하드 상한은 20,000자 | `textarea maxLength={700}`, `api/extract.ts`의 `MAX_TEXT` | 사용자는 지원 범위를 넘겨 입력할 수 없음. API의 더 큰 상한은 직접 호출에 대한 물리적 가드이며 제품 지원 약속이 아님 |
| 모델은 `gpt-5.6-sol`, 추론은 `medium` | Vercel 환경변수와 코드 기본값을 동일하게 맞춤 | Astra/high보다 비용과 지연을 줄임. 20260915-0212에서 5건 모두 22.4~40.2초에 완료, 손채점 38/41 |
| 1차는 동기 요청 유지 | 브라우저가 `/api/extract` 결과를 한 번에 기다림 | 구현과 설명이 단순함. 대신 모델 응답 편차가 시간 제한을 넘으면 다시 504가 날 수 있음 |
| 비동기 작업 큐·백그라운드 폴링은 보류 | DB·큐·작업 상태 API 없음 | 504를 구조적으로 없애는 방법이지만 포트폴리오 1차 범위와 비용·구현량을 넘음. 실제 서비스화할 때 가장 먼저 검토 |
| 결과를 저장하지 않음 | DB·로그인·회의 목록 없음 | 먼저 추출·검토 흐름의 유효성을 증명. 누적 검색은 다음 단계 |

### 2. 요청 경로와 배포 경계

| 결정 | 구현 | 이유와 주의 |
|---|---|---|
| React 19 + Vite SPA | `src/` | 비개발자가 브라우저에서 쓰는 단일 화면 도구 |
| Vercel Node Function 하나 | `api/extract.ts` | 요청 검증 → OpenAI 호출 → 후처리 → 응답만 담당하는 얇은 경계 |
| OpenAI Responses API + strict Structured Outputs | `lib/prompt.ts`의 JSON Schema | 자유 형식 파싱을 없애고 타입 모양을 강제. 스키마는 내용의 진실성까지 보장하지 않음 |
| 운영 설정은 환경변수, 안전한 기본값은 코드 | `OPENAI_MODEL`, `OPENAI_REASONING_EFFORT`, `OPENAI_API_KEY` | 실험은 배포 설정으로 빠르게 되돌리고, 환경변수가 빠져도 Sol/medium으로 동작 |
| 현재 시간 제한은 **OpenAI 55초 / 함수 60초** | `AbortSignal.timeout(55_000)`, `maxDuration: 60` | 45초는 과거 설정. Sol/medium은 1회 회귀에서 통과했지만 구조적 보장은 아님 |
| 현재 `max_output_tokens`는 24,000 | 추론 토큰과 보이는 JSON 출력의 합산 상한 | high 실험 때 올린 값. 실제 사용량을 기록한 뒤 낮출 후보이며, 너무 낮추면 504 대신 incomplete/502가 날 수 있음 |
| 프론트는 동일 출처 `/api/extract`만 호출 | 개발 시 Vite가 배포 API로 프록시 | CORS를 열어 다른 사이트가 서버의 API 키 비용을 쓰게 하지 않음. 로컬 API 검증은 `vercel dev` 필요 |
| 공유 키는 선택 | `EXTRACT_SHARED_KEY`가 있을 때만 `x-extract-key` 요구 | 현재는 공개 포트폴리오라 열려 있음. 실제 사내 배포에는 인증 필요 |

### 3. AI와 코드의 책임 분리

| 결정 | AI가 하는 일 | 코드가 하는 일 |
|---|---|---|
| AI는 발췌 중심 | 참석자·TPO·회의 날짜 원문, 결정·할 일·미결 항목과 인용문 추출 | 날짜 환산, 참석자 대조, 검토 사유, 환각 탐지, ID 생성 |
| AI 판단 칸은 제한 | `type`, `content`, `conditional`, `ambiguous_intent`, 안건 `title`, `summary` | 나머지 8개 검토 사유는 AI 값을 버리고 코드가 다시 판정 |
| 문장 작성 예외는 두 칸 | 안건 제목과 요약만 작성 가능 | 화면에서 "확인이 필요합니다"를 표시. 검증되지 않았다는 사실을 숨기지 않음 |
| 모든 항목은 원문 인용 필수 | `quote`, 번복 시 `supersededQuote` 제출 | 정규화한 원문에 실제 문자열이 있는지 확인하고 없으면 `rejected`로 분리 |
| 날짜는 발췌 후 코드 계산 | `meetingDateRaw`, `dueRaw`, `anchorRaw`만 추출 | 사용자 지정 > 원문 > fallback 날짜 우선순위와 절대·상대·역산 계산 |
| 담당자를 여러 발화에서 추론하지 않음 | 해당 발화에 드러난 이름만 제출 | 참석자 목록과 대조하고 동명이인·1인칭·근거 부족이면 비우고 되묻기 |
| API는 얇게 유지 | 모델 호출만 수행 | 도메인 로직은 `lib/`의 순수 함수로 분리해 LLM 없이 단위 테스트 |

### 4. 데이터 모델과 편집 구조

| 결정 | 구현 | 이유 |
|---|---|---|
| 안건(agenda) 아래 발화 항목(item) | 원시 스키마는 `agendas[].items[]`, 후처리는 평면 `items` + `agendaId` | 사람에게는 문맥 단위로 읽히면서 인용 검증과 CSV는 항목 단위 유지 |
| 관계의 단일 소유자는 항목 | `ProcessedAgenda`는 항목 ID 목록을 갖지 않고 항목만 `agendaId` 보유 | 양방향 목록 불일치 방지 |
| ID는 결정적으로 생성 | 같은 입력은 같은 항목 ID | 저장소 없이도 편집과 되돌리기에서 안정적인 식별자 제공 |
| 편집은 클라이언트 상태 | 수정·삭제·추가·분류 변경·되돌리기 | DB 없는 1차 범위에서 검토 가능성을 먼저 증명 |
| 결과 화면은 한 열의 회의록 문서 | 참석자 → TPO → 안건 → 항목 순서 | 칸반보다 실제 회의록을 읽는 순서를 우선 |
| 항목은 카드가 아니라 줄 | 분류·담당자·내용·기한·검토 사유를 한 줄에 배치 | 반복 정보와 세로 길이를 줄이고, 색·배지는 예외에만 사용 |

### 5. 검증·모듈·운영 규칙

| 결정 | 구현 | 이유와 주의 |
|---|---|---|
| 단위 테스트는 LLM 없이 | `tests/*.test.ts`, 현재 69개 | 날짜·대조·후처리 같은 결정적 코드를 빠르고 싸게 검증 |
| 회귀는 실제 배포본 5개 샘플 | `scripts/regression.mjs`, `docs/regression-runs/` | 모델 품질과 운영 시간은 실제 호출로만 확인. 호출마다 비용 발생 |
| 회귀 채점은 사람이 함 | `00_함정_정답지.md`와 대조, 현재 만점 41 | 안건 경계와 항목 누락은 단순 자동 채점으로 판단하기 어려움 |
| 속도와 품질을 분리 평가 | 먼저 504 여부, 그다음 손채점 | 빨라졌다는 사실이 추출 품질을 보장하지 않음 |
| 서버 ESM import는 `.js` 확장자 | 소스가 `.ts`여도 `../lib/date.js`처럼 작성 | Vercel Node 런타임은 TS를 변환하지만 번들하지 않음 |
| 프론트의 서버 타입 import는 `import type` | 컴파일 때 제거 | 프롬프트·API 키 관련 서버 코드가 브라우저 번들에 섞이는 것 방지 |
| TypeScript 프로젝트를 셋으로 분리 | app / node / server `tsconfig` | 프론트 bundler 규칙과 서버 NodeNext 규칙을 각각 적용 |

## 미결정

전부 닫혔습니다. 단 하나 남은 것:

**README 논거 2번 (섹션 6에서 결정)**
DB를 안 넣으므로 "결과가 누적된다"는 논거가 자기모순이 됩니다. 둘 중 하나:
- (A) 논거를 2개로 줄이고 "누적"을 빼기
- (B) **"누적은 다음 단계"로 명시하고 남기기 — 추천.**
  1차 범위를 스스로 그었다는 증거가 되고, 스키마가 이미 테이블 설계를 따라가도록
  잡혀 있다는 점(항목마다 결정적 `id`, 회의 메타데이터 분리)을 같이 말할 수 있음

## "왜 만들었나"에 대한 답 (README 2장용)

복붙해서 클로드에 넣으면 되는 일에 앱을 만드는 건 낭비다.
앱이 필요해지는 건 세 조건이 성립할 때다.

1. 여러 사람이 쓴다 → 프롬프트를 고정해야 결과 양식이 통일된다
2. 결과가 누적된다 → 채팅창에 흩어지면 "지난달 내 액션아이템"을 못 본다 (※ 위 (B) 참조)
3. 사내 정보를 외부 AI에 넣을 수 없다 → 사내 도구가 필요한 이유

---

## 현재 상태

### 진행도

| 섹션 | 상태 |
|---|---|
| 1. 스키마 확정 | ✅ 완료 |
| 2. API + 후처리 | ✅ 완료 |
| 3. 화면 (붙여넣기 → 미리보기) | ✅ 완료 — 5대 요소 문서 형태. 값 수정은 섹션 4 |
| 3.5 TPO + 논의 내용 | ✅ 완료 — 5대 요소가 전부 채워짐 |
| 3.9 회귀 5차 측정 | ✅ 29/31 — `docs/regression.md` |
| **4. 되묻기 / 편집** | ✅ 담당자·기한·안건 수정/삭제, 안건 추가, 분류 변경, 되돌리기. 금액 단위 확인은 후속 |
| **4.5 항목 단위를 안건으로** | ✅ 완료 + 배포 |
| 4.6 화면 — 카드를 줄로, 색 통일 | ✅ 완료 |
| **4.9 회귀 6차 측정** | ✅**38/41 — 안건 10/10.** 완료 |
| **5. 내보내기(마크다운/CSV)** | ✅ 완료 — 마크다운 복사 + CSV 내려받기 |
| 6. README | ⬜ |

### 배포

- https://minutes-to-actions-alpha.vercel.app/api/extract
- Vercel 환경변수: `OPENAI_API_KEY`, `OPENAI_MODEL=gpt-5.6-sol`,
  `OPENAI_REASONING_EFFORT=medium` 등록됨
- 코드 기본값도 배포 설정과 같은 `gpt-5.6-sol` + `medium`
- 선택: `EXTRACT_SHARED_KEY`를 넣으면 `x-extract-key` 헤더를 요구함. 지금은 열려 있음

요청:
```
POST /api/extract
{
  "text": "회의록 원문",
  "meetingDate": "2026-09-13",   // 사용자가 날짜칸을 직접 고쳤을 때만. 아니면 null
  "fallbackDate": "2026-09-14"   // 프론트가 채운 오늘. 원문에도 날짜가 없을 때만 씀
}
```
응답: `ProcessedMeeting` (`lib/postprocess.ts` 참조)

### 파일

```
api/extract.ts        요청 검증 → LLM 호출 → lib 후처리 → 응답. 얇게 유지
lib/types.ts          스키마 타입. ReviewReason 10종
lib/prompt.ts         SYSTEM_PROMPT + Structured Outputs 스키마
lib/verify.ts         quote / supersededQuote 환각 탐지
lib/date.ts           parseDue — 역산 > 절대 > 상대 순서
lib/people.ts         참석자 대조, 후보 제안, 1인칭·금액단위·인용문 검사
lib/postprocess.ts    후처리 조립(0단계 안건 펼치기 + 1~6단계) + CODE_OWNED + resolveMeetingDate
lib/labels.ts         사유·분류 → 한국어 라벨 + ask/warn/strike 구분. 내보내기 둘 다 여기를 씀
lib/export-markdown.ts 마크다운 내보내기. ResultDoc.tsx의 순서·제목을 그대로 따름 (섹션 5)
lib/export-csv.ts     CSV 내보내기. 안건 묶음을 펴서 한 항목 = 한 줄. BOM + CRLF (섹션 5)
src/App.tsx           붙여넣기 → 추출 → 결과. 기준일 칸
lib/edit.ts           editItem / createItem / editMeetingTpo / editMeetingAttendees.
                      담당자는 참석자 목록의 **사본**이라, 참석자를 고치면 여기서 맞춰줌
src/ResultDoc.tsx     결과를 회의록 문서 모양으로. 참석자 → TPO → 안건 묶음.
                      TpoSection·AttendeeSection의 인라인 편집이 여기 있음 (4.8)
src/ItemCard.tsx      항목 **한 줄**(카드 아님). 이름만 옛날 것. 수정·삭제 아이콘 포함
src/ItemForm.tsx      항목 수정·추가 폼. ItemModal 안에서 쓰임
src/ItemModal.tsx     항목 폼을 감싸는 <dialog>. showModal()이 포커스 가두기·ESC를 처리
src/MeetingEditor.tsx 편집 상태·되돌리기. adding 상태가 안건 id
src/App.css           .row* = 항목 줄, .agenda* = 안건, .tpo-* / .person* = 인라인 편집.
                      색은 강조색 하나 + 무채색
src/styles/tokens.css 색·간격·글자 토큰. 4px 배수가 규칙이고 예외는 주석에 이유를 적음
src/fixtures/sample01.ts  더미 결과. DEV에서만 쓰이고 프로덕션 번들에서 빠짐
tests/*.test.ts       69 passing. LLM 없이 가짜 응답으로 후처리 전체 검증

00_함정_정답지.md      각 샘플의 함정과 기대 동작 (AI 입력에 넣지 말 것)
extract-prompt.md     프롬프트 설계 문서 (3차). lib/prompt.ts와 짝
docs/regression.md    회귀 측정 기록. 점수 추이와 남은 실패 목록
docs/regression-runs/ 측정 실행 결과 원본. 스크립트가 만듦
scripts/regression.mjs 회귀 실행기. 채점은 안 하고 응답만 남김. 안건별 항목 수를 찍음
01~05_*.txt/md        더미 회의록 5개
```

`npm test` / `npm run typecheck` / `npm run build` / `npm run regression`. 테스트 러너는 `tsx`.
테스트는 **100 passing**입니다 (4.5에서 안건 10개, 섹션 5에서 내보내기 28개 추가).

### 화면만 고칠 때는 「더미 데이터 (01번)」 버튼

추출 버튼은 누를 때마다 OpenAI 토큰이 나갑니다. 줄 모양이나 사유 글씨를 손볼 때는
더미 버튼을 쓰십시오. 네트워크 호출이 0건인 것을 확인했습니다.

- `src/fixtures/sample01.ts` — `postprocess()`를 실제로 돌려 굳힌 값이라 화면이 받는 모양과 같음
  (빌드 후 `dist/assets/*.js`에 `SAMPLE_01`·원문 문자열 없음을 확인)
- 안건 4개는 **손으로 묶은 것**입니다. 프롬프트가 안건을 잘 가르는지는 이 파일로 모릅니다
- 항목 1~4만 AI 실제 출력, **나머지는 배지 확인용 합성**입니다.
  회귀 점수 근거로 쓰지 마십시오 — 그건 실제 추출로만 잽니다
- 스키마가 바뀌면 `tsc`가 이 파일에서 먼저 터집니다. 타입을 박아 둔 이유

### 회귀 점수 — 6차 38/41

마지막 측정은 **38/41 (6차)**. 자세한 내용은 `docs/regression.md`.

**만점이 31 → 41로 바뀌었습니다.** 샘플마다 «안건 수»·«안건 묶음» 두 줄을
정답지에 넣었기 때문입니다. **"29 → 몇 점"으로 읽지 마십시오.**
6차는 새 척도의 첫 측정입니다.

왜 만점을 다시 짰는지는 `00_함정_정답지.md`의 「왜 만점을 다시 짰나」에 있습니다.
요약하면: 04번이 옛 정답지로 거의 만점인데 결과물은 쓸 수 없었습니다.
**점수는 맞는데 쓸모가 없었고**, 정답지가 "무엇을 하나의 안건으로 볼 것인가"를
아예 안 재고 있어서 29/31로도 그 구멍이 안 보였습니다.
→ README 재료입니다: *측정 지표가 제품 품질과 어긋나 있던 지점*.

6차까지 확인된 것:

- 코드가 맡은 일(날짜 환산·역산·참석자 대조·환각 탐지)은 실행 **전부 정확**
- 환각 `rejected` **0건**
- 실패는 전원 AI 판단 칸에 몰려 있음
- ±1~2점은 편차. 항목 누락이 실행마다 흔들림

**6차 1차 시도 (20260914-1159) — 점수 미확정**

| 샘플 | 결과 |
|---|---|
| 01 | 43.1s · 항목 6개 (결정 1 / 할일 4 / 미결 1) · 검토필요 3 |
| 02 | 46.5s · 안건 2개 · 항목 4개 · 검토필요 4 · 환각 0 |
| 03·04·05 | **✗ HTTP 504** |

- **배포는 정상입니다.** 응답에 `agendas`가 들어 있습니다.
  이 실행의 `⚠ 구버전 배포입니다` 경고는 **스크립트가 옛날 기준**이라 뜬 것입니다
  (`discussionSummary`가 있는지 보고 있었는데 4.5에서 없앤 칸). 스크립트는 고쳤습니다
- 02번 손채점 **6/8** — 실패 2건:
  - «안건 수» 기대 1개 → 실제 2개 (「외주 업체 선정」/「계약 검토 및 착수」)
  - "3천 / 4천2백" 견적 비교가 **항목으로 안 잡혀** `unit_unclear`를 못 씀
- 02번 요약에 원문에 없는 숫자·이름 없음. 깨끗함
- **정답지 02번 «안건 수»가 너무 엄격할 수 있습니다.** 업체 선정과 계약·착수는
  다른 단계라 2개로 가른 것이 틀렸다고 보기 어렵습니다. 「1~2개 허용」으로
  고칠지 다음 세션에서 정하십시오

**6차 본측정 결과**

1. **안건 10/10.** 4.5의 목표인 안건 수와 묶음이 5개 샘플에서 모두 맞았습니다
2. 나머지 기존 항목은 **28/31**. 5차의 29/31보다 1점 낮아 편차 범위 안입니다
3. 01번 항목은 6개로 5차와 같습니다. 안건 계층을 올리면서 항목 수가 흔들리지 않았습니다

**재는 법**

```bash
npm run regression        # 5건을 배포본에 넣고 docs/regression-runs/<시각>/ 에 저장
```

- 채점은 `00_함정_정답지.md`와 대조해 **손으로** 합니다. 스크립트는 세지 않습니다
- 스크립트가 `⚠ 구버전 배포입니다`를 찍으면 점수를 재지 말고 배포부터.
  **단 "응답에 없는 칸: discussionSummary"라고 찍히면 스크립트가 옛것입니다** —
  4.5에서 `agendas` 기준으로 고쳤으니 이제 안 뜹니다
- **재기 전에 배포하십시오.** 옛 배포본은 최상위 `items`를 돌려주고,
  새 코드는 그걸 항목 0개로 읽습니다
- 안건 **제목·요약의 품질은 채점하지 않습니다.** 대조할 원문이 없습니다.
  단 요약에 원문에 없는 숫자·이름·날짜가 있으면 그 샘플의 «안건 묶음»을 0점 처리

---

## 이번에 배운 것 (README 트러블슈팅용)

**Vercel Node 런타임은 TS를 JS로 변환만 하고 번들하지 않습니다.**
그래서 배포되는 것은 `api/extract.js`이고, ESM 규칙상 상대 import에
확장자가 **반드시** 있어야 합니다. 게다가 소스는 `.ts`인데 import는 `.js`로 씁니다.

```ts
import { parseDue } from '../lib/date.js'   // 실제 파일은 date.ts
```

`.ts` 확장자 → 500 (`ERR_MODULE_NOT_FOUND`)
확장자 없음 → 500 (같은 에러)
`.js` 확장자 → 정상

두 번 틀린 뒤 **Vercel 런타임 로그를 열어서** 한 줄로 원인을 찾았습니다.
빌드 로그가 아니라 런타임 로그입니다 (프로젝트 → Logs).

---

---

## 해결됨 — 기준 날짜 우선순위 (2026-09-14)

(B)안으로 갔습니다. **사용자 지정 > 원문 > 오늘.**

우선순위를 아는 곳은 `lib/postprocess.ts`의 `resolveMeetingDate()` 한 함수뿐입니다.
`api/extract.ts`도 화면도 판단하지 않고 값만 넘깁니다.

| 출처 | 어디서 오나 | `meetingDateSource` |
|---|---|---|
| 사용자 지정 | 화면 날짜칸을 **직접 고쳤을 때만** `meetingDate`로 전송 | `user` |
| 원문 | AI가 뽑은 `meetingDateRaw`를 `parseAbsolute`가 환산 | `document` |
| 오늘 | 화면이 항상 `fallbackDate`로 같이 보냄 | `fallback` |
| 없음 | 셋 다 없음 — 기한 환산 안 함 | `none` |

고친 파일:

- `lib/postprocess.ts` — `resolveMeetingDate()` 신설. `postprocess(raw, text, userDate, fallbackDate)`.
  응답에 `meetingDateSource` 추가
- `lib/date.ts` — `parseAbsolute`가 `"2025년 11월 2일"`을 읽습니다.
  전에는 연도를 무시하고 base 연도로 덮었습니다
- `api/extract.ts` — body에 `fallbackDate`(YYYY-MM-DD, 선택) 검증 추가.
  `buildUserMessage`에는 **사용자 지정 날짜만** 넘깁니다 — 오늘 날짜를 "회의 날짜"라고
  알려주면 AI가 원문 대신 그걸 `meetingDateRaw`로 베낍니다
- `src/useExtract.ts` — `extract(text, meetingDate, fallbackDate)`
- `src/App.tsx` — `meetingDate` 상태를 `pickedDate: string | null`로 바꿈.
  날짜칸에 보이는 값은 `pickedDate ?? 결과의 기준일 ?? 오늘`.
  라벨이 출처별로 갈립니다("원문에 적힌 날짜를 기준으로…").
  기준일이 원문과 다르면 원문 표현을 옆에 남깁니다
- `tests/postprocess.test.ts` — **7개 추가 (44 → 51 passing)**

01번 샘플 실측 — 문서의 "맞는 값"과 일치합니다:

| | 전 | 지금 |
|---|---|---|
| `meetingDate` | 2026-09-14 (오늘) | **2026-09-08** (원문) |
| "이번 주 안에" | 2026-09-20 | **2026-09-13** |
| "담주 화요일까지" | 2026-09-22 | **2026-09-15** |

**왜 44개가 전부 통과했었나** — 테스트가 `meetingDate`를 항상 넘겨줬기 때문입니다.
실패하는 경로를 한 번도 안 탔습니다. 추가한 7개는 *안 넘기는* 경우를 만듭니다.
"모든 입력을 다 채운 테스트"는 기본값 버그를 절대 못 잡습니다. README 재료입니다.

### 딸려 나온 버그 — 프롬프트에 `meetingDateRaw` 지시가 없었음

화면에서 01번을 돌려 보고 찾았습니다. 원문 둘째 줄에 `2026-09-08 (월) 10:00`이
버젓이 적혀 있는데 AI가 `meetingDateRaw: null`을 냈습니다.

`EXTRACTION_SCHEMA`에는 `meetingDateRaw`가 required로 들어 있지만,
`SYSTEM_PROMPT`에는 **그 칸을 채우라는 말이 한 줄도 없었습니다.**
1단계 참석자 / 2단계 제외 / 3단계 항목 / 4단계 사유뿐이었습니다.

> **스키마에 필드를 넣는 것과, 채우라고 시키는 것은 다른 일입니다.**
> Structured Outputs는 타입만 강제합니다. 내용은 강제하지 않습니다. README 재료입니다.

고침: `lib/prompt.ts`에 **0단계 — 회의 날짜** 신설.
"다음 회의: 9/15(월)"을 회의 날짜로 착각하지 말라는 줄도 같이 넣었습니다
(01번 마지막 줄에 실제로 있습니다). `extract-prompt.md` 변경 이력 4차에 기록.

### 주의 — 프론트만 고치면 오히려 더 나빠집니다

`src/`는 로컬에서 바로 바뀌지만 `api/`는 **배포해야 바뀝니다.**
vite 프록시가 배포된 함수로 넘기기 때문입니다 (아래 "나중에 공부할 내용" 1번).

배포 전 상태 = 새 프론트(`meetingDate: null` + `fallbackDate`) + 구버전 서버
→ 서버가 `fallbackDate`를 모름 → 기준일이 **아예 없음** → 기한 전부 `due_unparseable`.
고치기 전보다 나빠 보입니다. 응답에 `meetingDateSource` 필드가 없으면 구버전입니다.
**계약(요청 body)을 바꿨으면 배포부터 하고 화면을 보십시오.**

---

## 해결됨 — TPO + 논의 내용 (2026-09-14, 섹션 3.5)

회의록 5대 필수 요소가 전부 채워졌습니다. 화면에 "아직 안 뽑음"이 없습니다.

**(1) TPO — `meetingTimeRaw` · `meetingPlaceRaw` · `purposeRaw`**

셋 다 발췌라 "AI는 발췌만" 원칙이 깨지지 않습니다. 코드는 통과만 시킵니다
(`blankToNull`로 빈 문자열을 null로 접는 것이 전부). 환산도 대조도 없으므로
`meetingDateSource` 같은 출처 표시도 없습니다 — 원문에 있거나 없거나 둘 뿐입니다.

`purposeRaw`에만 방어를 더 넣었습니다. 목적은 **회의 내용을 읽고 그럴듯하게
지어낼 수 있는 유일한 발췌 칸**이라, 찾는 위치를 고정했습니다:
제목 줄 → `"안건:"`·`"목적:"` 줄 → 없으면 null. "요약해서 만들지 마십시오"도 명시.

**(2) 논의 내용 — `discussionSummary`. (B)안**

> ⚠️ **이 칸은 섹션 4.5에서 없어졌습니다.** 안건별 `summary`가 대신합니다.
> 아래는 그때의 판단 기록으로 남깁니다 — "원칙을 통째로 지우지 않고 좁힌다"는
> 방법은 4.5에서도 그대로 썼습니다.

이 프로젝트에서 **발췌가 아닌 유일한 칸**이었습니다. `verify.ts`의 원문 대조를
받지 않으므로, 지어내도 코드가 못 잡습니다. 프롬프트가 유일한 방어선입니다.

- `SYSTEM_PROMPT` 첫 줄을 **지우지 않고 좁혔습니다** —
  "항목은 요약하지 않습니다 / 요약을 쓰는 칸은 discussionSummary 하나뿐"
  원칙을 통째로 없애면 AI가 다른 칸에서도 문장을 쓰기 시작합니다
- 5단계 신설. 금지 목록에서 제일 중요한 줄:
  **"결정·할 일·미결을 다시 나열하지 마십시오"** — 요약을 허용하면 AI가 가장
  먼저 하는 일이 항목 재나열입니다. 같은 내용이 화면에 두 번 나옵니다
- 화면은 이 칸에만 `.doc-unverified` 경고를 **요약보다 위에** 띄웁니다.
  읽고 난 뒤에 "확인이 필요합니다"를 보여주면 늦습니다

**고친 파일**

- `lib/types.ts` — `RawExtraction`에 4칸
- `lib/prompt.ts` — 0단계를 "회의 날짜" → "회의 머리말"로 확장, 5단계 신설,
  `EXTRACTION_SCHEMA` required 4개 추가
- `lib/postprocess.ts` — `ProcessedMeeting` 4칸 + `blankToNull()`
- `src/ResultDoc.tsx` — `<Unfilled/>` 제거, `<Field>` 신설, 논의 내용 섹션
- `src/App.css` — `.doc-unverified` (경고색). `.doc-todo`(회색)와 나눴습니다 —
  "없다"와 "있는데 못 믿는다"는 다른 상태입니다
- `src/fixtures/sample01.ts` — 4칸 **손으로** 채움 (재추출 아님. 주석에 명시)
- `api/extract.ts` — 기본 모델 `gpt-4.1-mini` → `gpt-6-astra`
- `tests/postprocess.test.ts` — **51 → 59 passing**
- `scripts/regression.mjs` · `package.json` — 회귀 실행기 신설

테스트 중 하나는 일부러 이렇게 이름 붙였습니다:

> `논의 내용은 환각 탐지를 타지 않는다 — 원문에 없어도 살아남는다`

이 칸이 검증을 안 받는 것은 **버그가 아니라 설계**입니다. 나중에 누가
"왜 여기만 verify를 안 타지?" 하고 고치려 들 때 이 테스트가 먼저 말을 겁니다.

4.5에서 이 테스트는 이름만 바꿔 살아남았습니다 —
`안건 제목·요약은 환각 탐지를 타지 않는다 — 원문에 없어도 살아남는다`.

---

## 남은 버그 — 담당자 대조 구멍 (급하지 않음)

01번 샘플에서 `"민수씨"` → 후보 `[]`. `"김 대리"` → `[김민수]` 는 됩니다.
`lib/people.ts` 가 **성은 보는데 이름은 안 봅니다.** 코드가 할 수 있는 일인데
되묻기로 넘어갑니다. 회귀 점수 올릴 때 손댈 항목.

---

## 나중에 공부할 내용

### 1. vite 프록시는 로컬 `api/`를 실행하지 않는다

한 번 착각했던 것이라 적어 둡니다.

```
브라우저 → localhost:5173/api/extract
         → vite dev 서버가 그대로 전달
         → minutes-to-actions-alpha.vercel.app/api/extract   ← 실제로 도는 건 여기
```

로컬 `api/extract.ts`는 가만히 있습니다. Vite는 프론트 번들러라
Vercel 함수를 돌리는 법을 모릅니다. 프록시가 하는 일은 **브라우저를 속이는 것**
뿐입니다 — "이 요청은 같은 출처야". 그래야 CORS가 안 뜹니다.

진짜로 로컬 `api/`를 돌리려면 `vercel dev` 입니다.

```bash
npm i -g vercel
vercel link
vercel env pull    # OPENAI_API_KEY 를 .env.local 로
vercel dev         # Vite 와 api/ 를 한 서버가 같이 서빙 → 프록시 불필요
```

지금 프록시를 고른 이유: `vercel dev`는 로그인·링크·키 내려받기가 붙고
함수가 콜드스타트해서 느립니다. 섹션 3은 화면 작업이라 서버를 건드릴 일이
없습니다. 서버를 고쳐야 할 때 `VITE_API_ORIGIN=http://localhost:3000 npm run dev`
로 바꾸면 됩니다. 그러라고 환경변수로 빼 뒀습니다.

**왜 CORS를 안 열었나** — `api/extract.ts`에 CORS 헤더를 열면 아무 사이트나
이 `OPENAI_API_KEY`로 요청을 쏠 수 있습니다. README 트러블슈팅 재료입니다.

찾아볼 키워드: `동일 출처 정책(Same-Origin Policy)`, `CORS preflight`,
`vite server.proxy`, `vercel dev`

### 2. `import type`은 번들에 남지 않는다

`src/useExtract.ts`가 `../lib/postprocess.js`에서 `ProcessedMeeting`을
가져오지만, 빌드 결과에 서버 코드는 들어가지 않습니다. `import type`은
타입만 쓰고 컴파일 때 지워지기 때문입니다 (`verbatimModuleSyntax: true`).

실제로 확인했습니다 — `dist/assets/*.js`에 `OPENAI_API_KEY`도
`SYSTEM_PROMPT`도 없습니다. 그냥 `import`로 쓰면 섞입니다. 타입만 필요할 때는
**반드시 `import type`** 입니다.

찾아볼 키워드: `verbatimModuleSyntax`, `type-only import`, `tree shaking`

### 3. tsconfig 프로젝트 참조

루트 `tsconfig.json`은 아무것도 컴파일하지 않고 참조만 합니다.

| 파일 | 대상 | moduleResolution |
|---|---|---|
| `tsconfig.app.json` | `src/` | bundler |
| `tsconfig.node.json` | `vite.config.ts` | nodenext |
| `tsconfig.server.json` | `lib/` `api/` `tests/` | nodenext |

프론트와 서버가 **모듈 해석 규칙이 다르기 때문에** 나눠야 합니다.
`npm run typecheck`(= `tsc -b`)가 셋을 한 번에 검사합니다.

알아 둘 것: `lib/`의 오류가 두 번 찍힙니다. server 프로젝트가 검사하고,
`src/`가 import 해서 app 프로젝트도 검사하기 때문입니다. 동작에는 문제 없습니다.

찾아볼 키워드: `TypeScript project references`, `tsc --build`

---

## 해결됨 — 섹션 4.5, 항목 단위를 안건으로 (2026-09-14)

**문제였던 것.** 04번을 돌리면 실제 안건은 3개인데 항목이 7개로 나왔습니다.

> "정다은 씨가 한 달치 질문 뽑아서 분류해주시고"
> — **무슨 질문을?** 항목만 보면 알 수 없었습니다. 답은 세 발화 앞에 있었습니다.

**원인은 프롬프트였습니다. AI가 못한 게 아닙니다.** 3단계 원칙 6·7·8 셋이
항목의 단위를 **발화**로 못박고 있었습니다. 이건 환각 탐지(quote 대조)를 쉽게
하려고 고른 단위지, 사람이 읽으라고 고른 단위가 아닙니다.

**고친 방법 — 단위를 바꾸지 않고 위에 한 겹을 올렸습니다.**

항목은 여전히 발화 단위입니다. `quote` 대조도 항목 단위 그대로 돌고,
섹션 5의 CSV 내보내기도 항목 단위 그대로 나갑니다.
달라진 건 **항목이 최상위가 아니라 안건 안에 들어간다**는 것뿐입니다.

```
### 1. 사내 문의 대응
슬랙 문의가 하루 10여 건, 절반 이상이 연차·경비 규정. 규정집을 공지해도 읽히지 않음.
  결정 · FAQ 제작
  할 일 · 정다은 — 한 달치 질문 분류 (여유 될 때 → 기한 확인 필요)
  미결 · 게시 위치. 노션 접근 불가자 있음
```

**확정한 것 두 가지** (지난 세션에서 미결로 남겼던 것)

| 물음 | 답 | 이유 |
|---|---|---|
| 안건 제목을 AI가 쓸까, 첫 발화를 그대로 쓸까 | **AI가 쓴다** | 첫 발화를 쓰면 검증 칸이 하나만 늘지만, 04번 제목이 "자 시작하죠. 오늘은 사내 문의 대응 건인데요"가 됨. 읽으라고 만든 칸이 안 읽힘 |
| 회의 전체 요약을 남길까 | **없앤다** | 안건별 요약과 같은 내용이 두 번 나옴 |

**설계 결정 — 안건은 항목 id 목록을 갖지 않습니다**

`ProcessedAgenda`는 `{ id, title, summary }`뿐이고, 소속은 각 항목의
`agendaId` **한 군데에만** 적힙니다. 양쪽에 적으면 사용자가 항목을 지웠을 때
두 곳을 같이 고쳐야 하고, 한쪽만 고치면 화면이 없는 항목을 그립니다.

**대가 — 감추지 않았습니다**

AI가 판단하는 칸이 **4개 → 6개**로 늘었습니다(`title`, `summary`).
검증 안 되는 면적이 넓어졌습니다.

다만 안건 묶음은 **틀리면 화면에서 바로 보입니다.** 상관없는 항목이 한 칸에
들어가 있으면 읽는 순간 압니다. `purposeRaw`·`discussionSummary`처럼 조용히
틀리는 종류가 아닙니다. 이 차이가 허용한 근거입니다.
화면은 안건 블록 맨 위에 `.doc-unverified` 경고를 띄웁니다 — 제목·요약뿐 아니라
**묶음 자체가 AI 판단**이라는 것까지 적었습니다.

**고친 파일**

| 파일 | 무엇을 |
|---|---|
| `lib/types.ts` | `RawAgenda` 신설. `RawExtraction`에서 `items`·`discussionSummary` 제거, `agendas` 추가 |
| `lib/prompt.ts` | 5단계 → 6단계. 3단계가 "항목 만들기"→"안건 가르기". 원칙 6·7·8 다시 씀. 6단계(제목·요약) 신설. 스키마 중첩 |
| `lib/postprocess.ts` | 0단계 신설 — 안건 안의 항목을 평면으로 펼치고 `Map<RawItem, agendaId>`로 소속 기억. `ProcessedAgenda` 신설 |
| `lib/edit.ts` | `createItem(id, **agendaId**, type, ...)`. 직접 추가한 항목도 반드시 안건에 속함 |
| `src/ResultDoc.tsx` | 칸반 3열 → 안건 묶음. 분류는 섹션 제목이 아니라 항목 라벨(`.type-tag`)로 |
| `src/MeetingEditor.tsx` | `adding` 상태가 `ItemType` → **안건 id**. 추가 버튼이 안건마다 |
| `src/App.css` | `.kanban-board` 계열 삭제, `.agenda*`·`.type-tag*` 신설 |
| `src/ItemForm.tsx` · `src/ItemCard.tsx` | "안건"이라 부르던 것을 "항목"으로 |
| `src/fixtures/sample01.ts` | 항목 6개를 안건 4개로 **손으로** 묶음 (재추출 아님) |
| `api/extract.ts` | `isRawExtraction`이 `v.items` → `v.agendas`를 봄 |
| `tests/postprocess.test.ts` | **59 → 69 passing.** `discussionSummary` 4건 제거, 안건 테스트 8건 추가 |
| `00_함정_정답지.md` | 샘플마다 «안건 수»·«안건 묶음». **만점 31 → 41** |
| `docs/regression.md` | 척도 변경 명시 + 6차 행 |
| `extract-prompt.md` | 7차 변경 이력 |

---

## 해결됨 — 화면, 카드를 줄로 (2026-09-14, 4.6)

**문제였던 것.** 4.5를 띄워 보니 세로가 **8072px**이었습니다. 스크롤 네 화면.
"할 일이 대단한 게 아닌데 왜 이렇게 크지"가 첫 반응이었습니다.

원인은 취향이 아니라 **밀도**였습니다.

- 테두리가 세 겹 — 안건 상자 → 항목 카드 → 원문 근거 상자
- 담당자·기한이 비어도 항상 그림. `—` 하나에 두 줄
- 원문 근거가 상시 노출. 대부분 `content`와 같은 문장이라 두 번 읽힘
- 수정·삭제 버튼 12개 + 초록 「항목 추가」 4개가 상시 노출
- 1440px 화면인데 내용은 720px. 오른쪽 절반이 빔

**고친 것 — 항목은 카드가 아니라 줄입니다.**

```
할 일   정다은 — 영수증 누락 4건 관련 사용자들에게 개별 연락 돌리기로.    › ✎ 🗑
```

- `분류 · 담당자 — 내용` + 오른쪽에 `기한 · 사유` + 아이콘 셋
- 원문 근거는 꺾쇠로 펼침. 기본 접힘
- 빈 담당자·기한은 안 그림. 단 **할 일인데 담당자가 없으면 "담당자 미정"** 표기
- 읽기 폭 720 → 960px
- 아이콘은 글리프(`✎`)가 아니라 **인라인 SVG**. 폰트에 따라 클립으로 보였습니다

**세로 8072px → 4906px.**

**색은 강조색 하나 + 무채색.**

노랑(경고)·빨강(삭제)을 없앴습니다. 색이 셋이면 "어느 색이 더 급한가"를
매번 해석해야 합니다. 위계는 색이 아니라 **채우기 / 테두리 / 글씨**로 냅니다.

그리고 **항목 줄 자체에는 색을 쓰지 않습니다.** 처음엔 `입력 필요` 줄에 배경을
깔았는데 6줄 중 4줄이 초록이 됐습니다. **예외 표시는 예외가 소수일 때만
작동합니다.** 사유 글씨가 이미 오른쪽 끝에서 말하고 있으니, 줄까지 칠하면
같은 말을 두 번 하는 것입니다.

> 이건 이 프로젝트가 이미 정한 **「배지는 예외에만 단다」를 상자와 색에도
> 적용한 것**입니다. 카드는 모든 항목에 똑같은 상자를 줘서 그 원칙을 어기고
> 있었습니다.

`tokens.css`의 warn·danger 토큰은 **안 지웠습니다.** App.css에서 덮어쓰기만
했으니 되돌리려면 그 블록만 지우면 됩니다.

**고친 파일**: `src/ItemCard.tsx` · `src/ResultDoc.tsx` · `src/App.css` 셋뿐입니다.
스키마·API·후처리는 안 건드렸습니다.

---

## 해결됨 — 응답 시간 초과 (504)

**5건 중 3건이 안 돌아옵니다.**

성공한 두 건이 **43.1초 / 46.5초**입니다. 타임아웃 55초, Vercel 함수 한도 60초.
**천장에 거의 닿아 있습니다.** 03·04·05는 넘겼습니다.

4.5에서 프롬프트가 5단계 → 6단계로 늘고 출력 스키마가 중첩되면서 추론이
무거워졌습니다. 지금 설정은 `api/extract.ts`에 있습니다.

```
:25   export const config = { maxDuration: 60 }
:116  reasoning: { effort: process.env.OPENAI_REASONING_EFFORT || 'high' }
:119  max_output_tokens: 24_000
:124  signal: AbortSignal.timeout(55_000)
```

**시도할 순서. 한 번에 하나씩입니다.**

| 순서 | 무엇을 | 왜 이 순서인가 |
|---|---|---|
| 1 | Vercel 환경변수 `OPENAI_REASONING_EFFORT=medium` → 재배포 → 재측정 | **코드를 안 고칩니다.** 프롬프트를 그대로 두고 이 값만 바꾸므로, 시간이 줄면서 점수가 얼마나 떨어지는지가 그대로 보입니다. 안 되면 `low` |
| 2 | `max_output_tokens`를 24000에서 낮추기 | 추론 토큰도 이 한도 안에서 씁니다. 다만 너무 낮추면 출력이 잘려 `status != 'completed'`로 502가 납니다 |
| 3 | 프롬프트 줄이기 — 6단계의 예시를 덜어내기 | 여기서부터는 점수에 직접 영향이 갑니다. 1·2로 안 되면 |
| 4 | `maxDuration`을 올리기 | Vercel 요금제에 달렸습니다. Hobby면 **60초가 천장**이라 못 올립니다 |

**하지 말 것:** 모델과 프롬프트를 같이 바꾸지 마십시오. 어느 쪽이 효과였는지
못 가립니다. 이건 이 프로젝트에서 이미 한 번 겪은 실수입니다
(「시도했다가 되돌린 것 — 추론 강도」 참조).

**해결 결과 (20260915-0212):** Vercel의 모델을 `gpt-5.6-sol`, 추론 강도를
`medium`으로 바꾼 뒤 01~05가 모두 완료됐습니다. 응답 시간은 각각 34.1초,
22.4초, 23.1초, 25.0초, 40.2초입니다. 손채점은 **38/41(92.7%)**이며,
안건 수·묶음은 전부 맞아 **10/10**입니다. 실패는 02 단위 모호 항목 누락,
03 1인칭 담당자 사유 오분류, 04 자발적 담당자 누락 세 건입니다.

---

## 해결됨 — 4.8 인라인 편집: TPO·참석자 (2026-09-15)

### 원칙 — 편집 모드가 레이아웃을 흔들면 안 된다

수정을 누른 순간 칸이 부풀면, **고치려던 줄이 눈앞에서 움직입니다.** 어디를
보고 있었는지 놓칩니다. 그래서 읽기 모드와 수정 모드가 **같은 DOM**입니다.

`<input>`을 항상 깔아둡니다. 읽기 모드의 input은 테두리가 `transparent`인 1px이라
그냥 글자로 보이고, 수정 모드에서는 그 1px에 **색만** 들어옵니다. 모드가 바뀔 때
달라지는 CSS 속성은 색과 `visibility`뿐이고, 여백·테두리 두께·글자 크기·줄 높이는
두 모드가 같은 값을 씁니다.

읽기 모드 input이 글자로 보이려면 **네 겹이 다 필요합니다.** 하나라도 빠지면
입력칸 티가 납니다. 하나씩 발견될 때마다 붙인 것이라 순서대로 적어둡니다.

1. `border-color: transparent` — 두께 1px은 **유지합니다.** 0으로 하면 수정 시 2px 자랍니다
2. `background: transparent`
3. `caret-color: transparent` — 테두리만 지우면 커서가 깜빡여서 여전히 입력칸입니다
4. `tabIndex={-1}` — 포커스 테두리를 지웠으므로, 탭이 여기 멈추면 어디인지 안 보입니다

### 함정 세 개 — 다시 밟지 마십시오

**1. `type=date`와 `type=text`는 내용 높이가 2px 다릅니다.**

수정 모드에서 날짜칸만 `date`로 바뀌는데, 그 2px 때문에 아래 세 줄과 안건 전체가
밀렸습니다. `.tpo-row__control`에 `height`를 못 박아 해결했습니다.

**2. 같은 자리의 버튼 두 개는 `key`가 없으면 DOM 노드가 재사용됩니다.**

「수정」과 「저장」이 같은 자리의 같은 `<button>`이라, React가 노드를 새로 만들지
않고 속성만 갈아끼웁니다. 그래서 「수정」을 누른 순간 그 노드의 `type`이
`button` → `submit`으로 바뀌고, **브라우저가 아직 처리 중이던 그 클릭의 기본 동작이
폼 제출**이 됩니다. 곧바로 `onSubmit`이 돌아 편집이 바로 닫힙니다 —
화면에는 "수정을 눌러도 아무 일도 안 일어난다"로 보입니다.

`key="edit"` / `key="save"`로 노드를 갈라놨습니다. **코드만 읽어서는 안 잡혔습니다.**
브라우저에서 최소 재현을 만들어서야 나왔습니다.

**3. `.doc-title`이 `sticky`입니다.**

버튼 줄을 제목 밖으로 꺼내면, 스크롤할 때 그 줄이 제목 밑으로 들어가 **클릭을
제목이 먹습니다.** 버튼 줄도 `sticky`로 붙여봤다가 **되돌렸습니다** — 버튼이 제목과
함께 화면 위에 계속 따라다녀서 훨씬 거슬립니다. 안 보이는 버튼을 누를 일은 없으므로
그냥 같이 스크롤되게 뒀습니다.

### 들어가고 나오는 법

- **진입** — 「수정」 버튼, 또는 값을 **더블클릭**. 더블클릭한 칸으로 커서가 갑니다.
  첫 칸 고정이면 「장소」를 눌렀는데 커서가 날짜로 튑니다
- **취소** — 입력 영역 **바깥 클릭** 또는 Esc. 취소 버튼은 두지 않았습니다
- **저장** — 「저장」 버튼 또는 Enter(폼 기본 동작)
- 바깥 클릭은 `click`이 아니라 **`pointerdown`**으로 잡습니다. 저장 버튼을 누를 때
  pointerdown이 먼저 오는데, 그 target이 폼 안이라 취소로 새지 않습니다.
  `click`으로 잡으면 저장이 안 먹습니다
- 더블클릭을 진입에 쓰면 **더블클릭 단어 선택을 잃습니다.** 알고 택한 것입니다.
  드래그와 세 번 클릭은 남습니다
- 읽기 모드 칸에 `title`로 "두 번 누르면 고칠 수 있어요"를 달아뒀습니다.
  버튼 말고는 더블클릭이 된다는 걸 알 방법이 없습니다

### 참석자 — 칩 + 담당자 연동

**한 줄 텍스트가 아니라 사람마다 칩입니다.** 이 앱을 쓰는 사람은 개발자가 아닙니다.
`이름(소속), 이름(소속)` 형식을 손으로 지키게 하면 쉼표나 괄호가 반드시 어긋납니다.
사람마다 칸을 나눠두면 형식을 지킬 일 자체가 없어집니다.

**세로 목록(ul/li)입니다.** 가로로 늘어놓으면 소속 없는 사람 뒤에 **빈칸이 구멍처럼**
보입니다 — 수정 모드의 소속 칸이 들어갈 자리를 읽기 모드에도 남겨둬야 하기
때문입니다(안 남기면 출렁입니다). 세로로 쌓으면 그 자리가 줄 끝으로 가서 안 보입니다.
글머리표(`·`)는 칩 테두리 **바깥**에 둡니다. 안에 넣으면 수정 모드 상자에 갇힙니다.

지우기(`×`)와 「+ 참석자」도 읽기 모드에 **자리를 잡아둡니다**(`visibility: hidden`).
`display`로 없애면 칩 폭과 줄바꿈이 달라집니다.

**`lib/edit.ts` — `editMeetingAttendees` / `AttendeeDraft`**

항목의 담당자는 참석자 목록을 **가리키는 참조가 아니라 값의 사본**입니다.
그래서 목록만 고치면 항목에는 없는 사람 이름이 조용히 남습니다. 여기서 맞춥니다.

`AttendeeDraft.originalIndex`가 핵심입니다. 이게 없으면 `김대리 → 김주임`이
**이름 변경**인지 `김대리 삭제 + 김주임 추가`인지 구분할 방법이 없는데,
둘은 담당자 처리가 정반대입니다.

- **이름 변경** → 그 사람이 담당이던 항목도 따라 바뀝니다
- **삭제** → 담당자를 비우고, 할 일이면 `no_assignee`(입력 필요)가 붙습니다

항목이 조용히 바뀌면 안 되므로 안내 문구를 나눠 띄웁니다 —
`참석자를 수정하고, 그 사람이 담당이던 항목도 함께 맞췄어요.` 되돌리기도 됩니다.

### 포커스 테두리 4px → 2px

`tokens.css`의 전역 `:focus-visible`이 `outline`을 `--space-1`(4px)로 두르고
있었습니다. 26px짜리 한 줄 입력칸에 4px을 두르면 칸이 부어 보입니다.

`--border-focus: 2px` 토큰을 신설했습니다. 4px 배수 규칙에서 벗어나는 값이라
`--border-thin`·스피너와 같은 **헤어라인 예외**로 주석에 이유를 적어뒀습니다.
전역 규칙이라 버튼·칩의 포커스 테두리도 함께 얇아집니다.

### 어떻게 확인했는가 — 레이아웃 측정

**눈으로는 두 번 놓쳤습니다.** "이 정도면 안 움직이겠지"로 판단해서 두 번 다
틀렸습니다. 레이아웃 시프트는 추론으로 잡히는 종류가 아닙니다.

헤드리스 크로미움에 **실제 `tokens.css` + `App.css`**를 물리고, 읽기/수정 두 상태의
모든 요소 `getBoundingClientRect()`를 재서 diff했습니다. 위 함정 1은 이렇게 나왔습니다.

```
row0  dh=+2    ← 날짜칸만 2px 자람
row1  dy=+2    ← 그래서 아래 세 줄이 통째로 내려감
```

이후 모든 변경에서 `no movement`(전 요소 dx·dy·dw·dh = 0)를 유지했습니다.
1013px와 420px 양쪽 다입니다.

React 동작(더블클릭 진입·포커스 대상·바깥 클릭 취소·저장·Esc)도 같은 방식으로
최소 재현을 만들어 확인했습니다. 함정 2는 이 방법으로만 잡혔습니다.

**측정 스크립트는 레포에 없습니다.** 클라우드 임시 폴더에서 돌렸습니다.
다시 만들려면 — 두 모드의 마크업을 그대로 뱉는 정적 HTML + playwright로
`getBoundingClientRect` diff. `tests/`에 넣어 회귀로 쓸지는 **미정**입니다.

### 논의만 하고 안 한 것 — 바깥 클릭 = 저장

「수정」 버튼을 없애고 더블클릭 진입 + 바깥 클릭 저장으로 가는 안을 검토했습니다.
방향은 괜찮은데 가드 세 개가 먼저 필요합니다. **지금 그대로 하면 앱이 죽습니다.**

1. **빈 이름** — 「+ 참석자」로 추가하고 이름을 안 적은 채 바깥을 누르면
   `editMeetingAttendees`가 throw하는데 `MeetingEditor`에 try/catch가 없습니다.
   지금은 저장 버튼 경로라 브라우저 `required`가 막아주고 있을 뿐입니다
2. **변경이 없을 때** — 읽기만 하고 나가도 "변경된 내용이 없어요"가 매번 뜹니다.
   조용히 닫아야 합니다
3. **모바일** — 더블탭은 브라우저 확대라 안 먹습니다. 「수정」 버튼이 사라지면
   터치에서 수정 자체가 불가능해집니다. 터치는 한 번 탭으로 나눠야 합니다

버튼 줄도 통째로 없애야 합니다. 「수정」만 빼고 「저장」을 남기면 모드마다 버튼 줄
높이가 달라져서 **출렁임 0이 깨집니다.**

---

## 다음 할 일

### 2순위 — 504 해결 재현 확인

`gpt-5.6-sol` + `medium`에서 5건이 한 번 모두 완료됐습니다. 같은 설정으로
`npm run regression`을 한 번 더 실행해 일시적인 편차가 아닌지 확인합니다.
두 번째 실행도 모두 55초 안에 끝나면 완료로 닫습니다.

**완료 기준:** 회귀 샘플 01~05가 연속된 두 번의 실행에서 모두 504 없이 완료됩니다.
속도만 보고 끝내지 말고, 그 설정을 유지한 채 2순위 회귀 채점으로 품질 저하를
확인합니다.

### 완료 — 회귀 6차 본측정

**38/41.** 안건 10/10, 기존 항목 28/31입니다. 5차의 기존 항목 29/31보다
1점 낮아 모델 편차 범위 안입니다. 상세 실패는 `docs/regression.md`에 있습니다.

### 완료 — 수정 폼을 화면 중앙 모달로

`src/ItemModal.tsx`(`<dialog>` + `showModal()`)로 끝났습니다. 제목에 어느 안건인지
적습니다. **항목만 모달이고 TPO·참석자는 인라인(4.8)이라 방식이 갈려 있습니다.**

## 해결됨 — 섹션 5, 내보내기 (2026-09-15)

`lib/export-markdown.ts`와 `lib/export-csv.ts`. 둘 다 순수 함수이고 DOM·클립보드를
모릅니다. 버튼은 `src/MeetingEditor.tsx`의 `.actions` 줄에 있습니다.

**이번에 정한 것**

| 결정 | 이유 |
|---|---|
| **마크다운은 클립보드 복사, CSV는 파일 내려받기** | 마크다운이 갈 곳은 노션 붙여넣기, CSV가 갈 곳은 엑셀입니다. 엑셀은 클립보드가 아니라 파일을 엽니다 |
| **사용자가 고친 값을 내보내기에 표시하지 않음** | 문서를 받는 사람에게 필요한 건 최종값입니다. 누가 고쳤는지는 화면에서 이미 보입니다 |
| **안건 제목·요약 경고는 문서에도 남김** | 위와 달리 이건 편집 이력이 아니라 **값의 신뢰도**입니다. 문서만 받은 사람에게도 필요합니다 |
| **CSV는 전체 항목** | 결정·할 일·미결을 다 넣고 「분류」 컬럼으로 거릅니다. 빼고 내보내면 엑셀에서 다시 붙일 방법이 없습니다 |
| **CSV에 인용문 컬럼 포함** | quote를 필수로 둔 이유(환각 탐지)와 결이 같습니다. 셀이 길어지는 건 감수합니다 |
| **BOM + CRLF** | BOM이 없으면 엑셀이 한글을 깨서 엽니다. CRLF는 RFC 4180 |

**지킨 규칙 — 되돌리지 마십시오**

- 마크다운의 섹션 순서·제목은 `ResultDoc.tsx`와 같습니다. 어긋나면 하나가 거짓말이 됩니다
- 빈 칸도 제목을 남깁니다 (`원문에 없음`). 장소가 원문에 없다는 것과 장소 칸이 문서에
  없는 것은 다릅니다
- 라벨은 `lib/labels.ts` 단일 출처. 두 파일 어디에도 한국어 사유 문자열이 없습니다
- 번복된 항목은 배지가 아니라 취소선(`~~`)입니다. 화면과 같은 규칙입니다
- 담당자 없는 **할 일**은 빈칸이 아니라 "담당자 미정"입니다. 엑셀에서 빈칸은
  "안 적힌 것"과 "안 정해진 것"을 구별하지 못합니다. 미결은 원래 담당자가 없으므로 빈칸

**안 한 것**

- CSV 수식 주입 가드 (`=`·`+`·`-`·`@`로 시작하는 셀). 엑셀이 수식으로 읽을 수 있습니다.
  막으려면 셀 앞에 `'`를 붙이는데 그 따옴표가 화면에 보입니다. 판단이 필요해 남겨뒀습니다
- 마크다운 `.md` 파일 내려받기 (클립보드만)

### 1순위 — 섹션 6, README

위 「미결정」의 논거 A/B를 먼저 정하십시오.

### 그 뒤

- (선택) CSV 수식 주입 가드
- (선택) `lib/people.ts` 이름 대조 구멍 — `"민수씨"` → 후보 `[]`

### 섹션 5 전후에 할 일

- ~~수정 전용 컴포넌트 만들기~~ → **완료** (항목은 모달, TPO·참석자는 인라인 4.8)
- **안건 제목·요약 수정 기능** (4.5에서 안 했음).
  검증 안 되는 칸인데 사람이 고칠 방법이 없습니다. 이 프로젝트의 "되묻기" 원칙과 어긋나므로 넣는 게 맞습니다. 다만 4.5 범위를 넘어서 미뤘습니다
- 45초 이상 지났을 때 해결 방법 — 아래 「되돌린 것」 참조. 천장이 60초입니다(완료)
- 토큰 최대 사용량 리밋 지정 (완료)

### 완료 (2026-09-14~15)

- ✅ 4.8 인라인 편집 — TPO·참석자를 읽기 화면 그 자리에서 수정 (출렁임 0)
- ✅ 참석자 편집 + 담당자 연동 (`lib/edit.ts`의 `editMeetingAttendees`)
- ✅ 항목 수정 폼을 `<dialog>` 모달로 (`src/ItemModal.tsx`)
- ✅ 포커스 테두리 4px → 2px (`--border-focus` 신설)
- ✅ 섹션 4.5 — 항목 단위를 안건으로 + 배포
- ✅ 4.6 화면 — 카드를 줄로, 색 통일 (세로 8072 → 4906px)
- ✅ 4.7 화면 — 결정·할 일·미결과 검토 사유를 뱃지로 구분, 불확실한 사유는
  노란색으로 표시, 모바일 우측 정렬, 항목 추가 버튼 전체 너비로 확대
- ✅ `scripts/regression.mjs` 구버전 판정을 `agendas` 기준으로 수정
- ✅ 추출 중 로딩 스피너 (`src/App.tsx` + `.spinner`. `--duration-slow` 토큰 신설)
- ✅ 모바일 섹션 제목 상단 고정 → **4.5에서 모든 폭으로 확대**
  (칸반이 없어져 화면이 항상 한 열이므로 폭으로 나눌 이유가 없어졌습니다)

---

## 다음 세션에 붙여넣을 말

```
포트폴리오 프로젝트 "회의록 추출기" 이어서 합니다.
폴더는 연결돼 있고, 00_인수인계.md를 먼저 읽어주세요.

504는 gpt-5.6-sol + reasoning medium에서 회귀 5건 모두 완료되어 해결됐습니다.
6차 손채점은 38/41, 안건 평가는 10/10입니다.
수정 폼 모달과 4.8 인라인 편집(TPO·참석자)은 끝났습니다.
섹션 5 내보내기(마크다운 복사 · CSV 내려받기)도 끝났습니다. npm test 100 passing.
이번 세션 목표는 **섹션 6 README**입니다.

순서:
1. 「미결정」의 README 논거 A/B를 먼저 정하기
2. 섹션 6 README 작성
3. 여유가 되면 같은 설정으로 npm run regression 재현 확인

지켜야 할 것:
- 한 번에 하나씩. 모델/추론강도와 프롬프트를 같이 바꾸지 마세요.
  어느 쪽이 효과였는지 못 가립니다. 이미 한 번 겪은 실수입니다.
- AI는 발췌만, 계산·대조·판단은 코드가 합니다.
  예외는 화면에 "확인이 필요합니다"를 붙인 칸뿐 — 안건 제목과 요약 둘입니다.
- 원칙을 통째로 지우지 마세요. 좁히세요.
- 용어: 안건 = 화제 하나(agenda), 항목 = 그 안의 결정·할 일·미결(item).
- 화면: 항목은 카드가 아니라 줄입니다. 손댈 게 없는 줄에는 색도 상자도 없습니다.
  "배지는 예외에만 단다"를 상자와 색에도 적용했습니다. 되돌리지 마세요.
- 편집 모드가 레이아웃을 흔들면 안 됩니다(4.8). 읽기/수정이 같은 DOM이고
  바뀌는 건 색과 visibility뿐입니다. 고치기 전에 「4.8 인라인 편집」의 함정 셋을
  읽으세요 — type=date 2px, 버튼 key, .doc-title sticky.
- 스키마를 바꾸면 tsc가 src/fixtures/sample01.ts에서 먼저 터집니다. 거기부터.
- 스키마를 바꾸면 scripts/regression.mjs도 같이 고치세요. 4.5에서 빠뜨렸습니다.
- 회귀 만점은 41입니다. 5차까지의 29/31과 직접 비교하지 마세요.
- 화면만 볼 때는 「더미 데이터 (01번)」 버튼을 쓰세요. 추출은 토큰이 나갑니다.
- 서버(api/, lib/)를 고쳤으면 배포부터 하고 화면을 보세요.
- 셸에서 git을 돌리지 마세요. index.lock이 남아 제 git이 막힙니다.
  npm run build 대신 npx vite build --outDir $HOME/build-check --emptyOutDir 를 쓰세요.
  임시 파일은 폴더 안이 아니라 $HOME에 만드세요 — 셸이 파일을 못 지웁니다.
- import는 소스가 .ts여도 '.js' 확장자로 씁니다.
- 제 편집기에 열려 있는 파일은 먼저 닫으라고 말해주세요. 덮어써진 적이 있습니다.
- 파일을 쓸 때마다 디스크에서 바이트 수를 확인하고 말해주세요.
  같은 전송 경로를 재사용하면 옛 내용이 가는 일이 있었습니다.

끝나면 docs/regression.md와 이 파일(00_인수인계.md)의 진행도 표를 갱신해주세요.
```

---

## 시도했다가 되돌린 것 — 추론 강도 (2026-09-14)

"항목이 잘게 쪼개지는 건 AI가 덜 생각해서다"라는 가설로 `api/extract.ts`에
`reasoning: { effort: 'high' }`와 `max_output_tokens: 24000`을 넣었습니다.

**결과: 504.** 45초 타임아웃을 넘겼습니다. 55초로 올려봤지만
`export const config = { maxDuration: 60 }`이 천장이라 더 못 올립니다.

그 사이에 진짜 원인이 프롬프트의 **항목 단위**라는 것이 드러나, 전부 원복했습니다.
현재 `api/extract.ts`는 이 시도 이전과 같습니다 (`max_output_tokens: 8000`, 45초, `reasoning` 없음).

**남는 것 두 가지**

1. `max_output_tokens`는 **추론 토큰까지 포함**합니다. effort를 올리면서
   이걸 안 올리면 추론이 한도를 먹고 출력이 잘려 `status`가 `completed`로 안 옵니다
2. **서버리스 함수 한도(60초)가 LLM 추론 시간의 천장입니다.** 추론을 쓰는 방향으로
   가려면 스트리밍이나 백그라운드 처리가 필요합니다. 「섹션 5전에 할 일」의
   45초 항목이 여기에 걸립니다. README 트러블슈팅 재료입니다

**순서를 잘못 잡았던 기록** — 같은 대화에서 "안건이 3개인데 7개로 나온다"는
정확한 관찰이 이미 나와 있었는데, "추론 레벨이 낮은 것 같다"를 먼저 쫓았습니다.
**증상(느리다·이상하다)보다 관찰(단위가 틀렸다)을 먼저 보십시오.**

---

## 작업 환경 주의 — 원격 셸의 제약 (2026-09-15 확인)

Claude의 셸은 유성 님 맥이 아니라 **폴더만 마운트된 리눅스**에서 돕니다.
여기서 나온 제약 넷입니다. 다음 세션에서 다시 부딪힐 것이므로 먼저 읽으십시오.

| 명령 | 되는가 | 이유와 대안 |
|---|---|---|
| `npm test` | ✅ | 단, `@esbuild/linux-arm64`를 `--no-save`로 **추가** 설치해 둔 상태라야 합니다. darwin 바이너리는 그대로 남아 있어 맥에서도 동일하게 돕니다 |
| `npm run typecheck` | ✅ | `tsc`는 순수 JS라 플랫폼을 안 탑니다 |
| `npm run build` | ❌ | `dist/`의 옛 파일을 **지우지 못합니다**. 대신 `npx vite build --outDir $HOME/build-check --emptyOutDir`로 검증하십시오. 몇 번이고 반복됩니다 |
| `git` | ❌ | `.git/index.lock`을 못 지워서 **유성 님 git이 막힙니다.** 셸에서 git을 돌리지 마십시오. 커밋은 유성 님이 직접 |

**`npm install`을 그냥 돌리면 안 됩니다.** darwin 바이너리를 리눅스 것으로 갈아치워
유성 님 로컬 `npm test`·`npm run dev`가 깨집니다. 꼭 필요하면 `--no-save`로
플랫폼 패키지만 **추가**하십시오.

**셸은 파일을 지우지 못합니다.** 임시 파일은 폴더 안이 아니라 `$HOME`(마운트 바깥)에
만드십시오. 폴더 안에 만들면 유성 님이 손으로 지워야 합니다.

---

## 작업 환경 주의 — 편집기가 열어둔 파일

Claude가 원격에서 파일을 쓰고 유성 님이 로컬 편집기로 여는 구조라, **편집기에
열려 있는 파일은 옛날 버퍼로 덮어써집니다.** 이번에 `src/App.css`가 실제로
두 번 날아갔습니다 (파일 크기가 그대로인 것으로 확인).

→ Claude에게 파일 수정을 맡길 때는 **그 파일 탭을 먼저 닫으십시오.**
→ 반영됐는지는 `grep`으로 확인하는 게 확실합니다.

**2026-09-15 추가.** 이번 세션에도 같은 증상이 두 번 났습니다. "썼다"는 응답이
돌아왔는데 디스크는 옛 크기 그대로였습니다. 그리고 **Claude가 파일을 다시 읽어
확인하는 경로도 캐시된 사본을 돌려줬습니다** — 이미 들어간 파일을 "안 들어갔다"고
읽고 불필요하게 덮어쓴 일이 있었습니다.

→ 확인은 파일을 다시 읽지 말고 **디렉터리 목록의 크기·수정시각**으로 하십시오.
   이쪽은 한 번도 틀리지 않았습니다.
→ 전송이 안 먹으면 **보내는 파일 이름을 바꿔서** 다시 보내면 들어갑니다.

---

## 못 한 것 — 2026-09-14 세션

**1. 회귀 6차 점수가 없습니다.** 위 「막힌 것」이 이유입니다. 504부터입니다.

**2. 수정 폼이 아직 줄 자리에서 펼쳐집니다.** → **해결됨.** `src/ItemModal.tsx`

**3. 안건 제목·요약을 사람이 고칠 방법이 없습니다.**

검증 안 되는 칸인데 수정 UI가 없습니다. 이 프로젝트의 "되묻기" 원칙과
어긋나지만 미뤘습니다. 모달 작업(3순위)에 얹으면 자연스럽습니다.

**4. `docs/editing.md`를 갱신하지 않았습니다.**

항목이 카드에서 줄로 바뀌고 「항목 추가」가 안건마다로 옮겨졌는데 편집 안내는
그대로입니다. 모달 작업을 하면 어차피 다시 써야 하니 그때 같이 고치십시오.

**5. 다크 모드를 확인하지 않았습니다.**

색을 강조색 하나로 통일하면서 warn·danger 사용처를 전부 덮어썼습니다.
`tokens.css`에 다크 팔레트가 있으니 깨지진 않을 텐데, 눈으로는 안 봤습니다.

---

## 이번 세션에서 못 한 것 (2026-09-15, 4.8)

**1. 타입체크·테스트·빌드를 돌리지 못했습니다.**

이번에도 클로드 쪽 셸이 연결 폴더를 마운트하지 못했습니다. `esbuild`로 문법
파싱만 통과시켰습니다. **`npm run typecheck`와 `npm test`를 한 번 돌려주십시오.**
특히 `lib/edit.ts`에 `editMeetingAttendees`가 새로 들어갔고,
`ResultDoc.tsx`의 `Section`에서 `action` prop을 뺐습니다(`noUnusedLocals: true`).

**2. 회귀 측정은 이 세션에서 하지 않았습니다.**

위의 6차 38/41은 **다른 세션에서 낸 값입니다.** 이번 작업(4.8)은 화면과
`lib/edit.ts`만 건드렸고 프롬프트·후처리는 그대로라 점수에 영향이 없어야 하지만,
확인은 안 했습니다.

**3. 참석자 편집에 테스트가 없습니다.**

`editMeetingAttendees`는 담당자를 따라 바꾸는 로직이라 조용히 틀릴 수 있는
자리입니다. 이름 변경·삭제·동명이인 세 경우를 `tests/`에 넣어야 합니다.

**4. 동명이인이 완전히 같아지는 경우를 막지 않았습니다.**

`김대리(기획)` / `김대리(영업)`에서 소속을 지우면 둘이 구분되지 않고,
담당자 대조(`sameAttendee`)가 첫 번째 사람을 잡습니다. 저장할 때 막을지 미정입니다.

**5. 다크 모드는 스크린샷으로만 봤습니다.**

측정용 헤드리스 브라우저를 다크로 띄워 확인했습니다. 실제 기기에서는 안 봤습니다.

---
