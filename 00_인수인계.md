# 회의록 추출기 — 인수인계 문서

> 새 대화를 시작할 때 이 파일을 먼저 올리세요.
> 최종 갱신: 2026-09-14 (섹션 1 완료 시점)

## 배경

- 경영지원팀 **AI 업무자동화 엔지니어** 직무 지원용 포트폴리오
- JD 키워드: 회의록 정리·보고서 자동화, 사내 챗봇, LLM API 활용, 업무 프로세스 분석을 통한 **자동화 과제 발굴**
- 기존 포트폴리오: 타임블록 (React 19 + Vite + Supabase + Vercel, 자연어 → 일정 구조화)
  - 재사용할 것: `api/parse-task.js` 구조, Structured Outputs, 미리보기→수정→저장 흐름
- 목표 기간: **1일** (45분 × 5섹션). 길어도 3일

## 확정된 판단

| 판단 | 이유 |
|---|---|
| **텍스트 입력 전용. 녹음 안 함** | 클로바노트·다글로·티로는 음성 처리 도구. 한국어 STT로 경쟁하면 짐. 텍스트는 겹치지 않음 |
| **웹앱으로 만듦 (스크립트 아님)** | 쓸 사람이 비개발자. 스크립트면 자동화가 아니라 대신 돌려주는 일 |
| **HWP 안 함** | 사내 회의록은 HWP를 안 씀. HWP는 대외·공공 제출용 |
| **출력 2갈래** | 회의록 본문 → 마크다운(노션 복붙) / 액션아이템 → CSV(엑셀 필터) |
| **AI는 발췌만, 계산은 코드** | 날짜·이름을 AI가 변환하면 틀려도 티가 안 남 |
| **quote 필수** | 원문 인용을 코드로 문자열 검사 → 환각 탐지 |
| **`type` 3종 유지** (decision/action/open) | 샘플 02의 번복된 결정이 갈 곳이 open뿐. 2종으로 줄이면 핵심 함정이 사라짐 |
| **검토 사유는 enum** | 자유 문장이면 사람마다 결과 양식이 달라짐. 코드가 UX 분기를 못 함 |
| **추론해서 채우지 않음** | 여러 발화에 걸친 담당자는 비우고 되묻기. 채우면 틀려도 티가 안 남 |
| **DB 저장 안 함 (1차)** | 먼저 간단히 완성해서 실물을 보고 판단. 목표 기간 1일을 지키는 쪽 |
| **로직은 `lib/`로 분리** | 후처리 6단계를 API 안에 다 넣으면 두꺼워지고 테스트가 안 됨 |

## 미결정

전부 닫혔습니다.

1. ~~저장(DB) 넣을지~~ → **1차에서는 안 넣음.**
   먼저 간단히 완성해서 실물을 보고 수정하기로. 나중에 붙일 수 있게
   API 응답 구조만 DB를 염두에 두고 잡아둘 것.
2. ~~`type` 3종 vs 2종~~ → **3종 확정** (샘플 02 근거)
3. ~~회의록 본문 정리본의 범위~~ → **항목 추출에 집중, 원문 윤문은 안 함.**
   참석자·안건 수준의 머리말만. (섹션 5 내보내기에서 최종 확인)

### 남은 판단 (섹션 6, README 쓸 때)

DB를 안 넣으면 아래 "왜 만들었나" 3개 논거 중 **2번이 자기모순**이 됩니다.
둘 중 하나를 고를 것:

- (A) 논거를 2개로 줄이고 "누적"은 아예 빼기 — 깔끔하지만 핵심 가치 하나를 버림
- (B) "누적은 다음 단계"로 명시하고 남기기 — **추천.**
  1차 범위를 스스로 그었다는 증거가 되고, 면접에서 "다음에 뭘 할 건가"에
  바로 답이 됨. 스키마가 이미 테이블 설계를 따라가도록 확정돼 있다는 점도 같이 말할 수 있음

## "왜 만들었나"에 대한 답 (README 2장용)

복붙해서 클로드에 넣으면 되는 일에 앱을 만드는 건 낭비다.
앱이 필요해지는 건 세 조건이 성립할 때다.

1. 여러 사람이 쓴다 → 프롬프트를 고정해야 결과 양식이 통일된다
2. 결과가 누적된다 → 채팅창에 흩어지면 "지난달 내 액션아이템"을 못 본다
3. 사내 정보를 외부 AI에 넣을 수 없다 → 사내 도구가 필요한 이유

> 2번은 DB 결정과 직결됩니다. DB를 안 넣기로 하면 이 논거를 수정해야 합니다.

## 파일

- `01~05_*.txt/md` — 더미 회의록 5개 (함정 의도적으로 삽입)
- `00_함정_정답지.md` — 각 샘플의 함정과 기대 동작 (**AI 입력에 넣지 말 것**)
  - 스키마와 짝을 이룸. 스키마가 바뀌면 여기도 같이 고칠 것
- `extract-prompt.md` — 시스템 프롬프트 + 출력 스키마 (**2차 확정본**)

## 지금까지 한 일

**섹션 1 — 스키마 확정 (완료)**

1. 1차 프롬프트로 샘플 02 테스트 → 6개 함정 중 3개 실패
2. 스키마 1차 개정 — 실패 원인이 전부 "담을 필드가 없음"이었음
3. 01~05 전수 회귀 확인 → 새 결함 4개 발견
4. 스키마 2차 개정 + 정답지 동기화

**최종 결과: 01 7/7, 02 6/6, 03 5/5, 04 5/6(설계상 의도), 05 5/5**

04번 1건은 "못 잡는 것"이 아니라 **여러 발화에 걸친 담당자를 추론하지 않기로 한 결정**.
`quote`를 배열로 만들면 환각 탐지가 복잡해지므로, 비우고 되묻는 쪽을 택함.

## 다음 할 일

1. **미결정 1번(DB) 결정** — 이게 1일이냐 3일이냐를 가름
2. **섹션 2 — API** (`api/extract.js`)
   - 타임블록의 `api/parse-task.js` 구조 재사용
   - Structured Outputs로 `extract-prompt.md`의 스키마 그대로
   - 응답 전에 **코드 후처리**: quote 환각 검사 → 날짜 파싱 → 참석자 대조 → reviewReasons 승격
3. **섹션 3 — 화면** (붙여넣기 → 미리보기 → 수정 → 저장)
4. **섹션 4 — 노란 표시 + 되묻기** (`reviewReasons` → UX 분기, `supersededQuote` 취소선)
5. **섹션 5 — 내보내기(마크다운/CSV) + 배포**
6. **README** (별도 2섹션)

## 다음 세션에 붙여넣을 말

```
포트폴리오 프로젝트 "회의록 추출기" 이어서 합니다.
아래 3개 파일 올렸습니다.

- 00_인수인계.md  ← 전체 맥락. 먼저 읽어주세요
- extract-prompt.md  ← 확정된 프롬프트 + 출력 스키마 (2차)
- 05_비품_예산_0913.txt  ← 테스트용 샘플 (가장 까다로운 것)

※ 00_함정_정답지.md는 일부러 안 올립니다. AI 입력에 넣으면 안 되는 파일입니다.

이번 세션 목표: 섹션 2 — API 구현 (45분)

## 만들 구조

api/extract.js      ← 얇게. 요청 검증 → LLM 호출 → lib 후처리 호출 → 응답
lib/verify.js       ← quote 환각 검사
lib/date.js         ← parseDue(dueDateRaw, anchorDateRaw ?? meetingDate)
lib/people.js       ← 참석자 대조, 후보 제안, duplicate_name
lib/postprocess.js  ← 위 셋을 순서대로 돌리고 reviewReasons 승격 + confidence 파생

로직을 lib/로 빼는 이유: API 안에 다 넣으면 두꺼워지고 테스트가 안 됩니다.
lib/는 순수 함수로 만들어서 LLM 호출 없이 단위 테스트가 가능해야 합니다.
(회의록 원문 + 가짜 LLM 응답 → 기대 결과, 형태로)

## 후처리 순서 (lib/postprocess.js)

1) quote / supersededQuote가 원문에 실제 존재하는지 문자열 포함 검사 → 환각 탐지
2) parseDue(dueDateRaw, anchorDateRaw ?? meetingDate) — 상대날짜·역산
   실패하면 due_unparseable 승격
3) assigneeRaw를 attendeesRaw와 대조, 실패 시 assignee_unmatched 승격
4) 후보 제안 — 성씨·이름 부분일치.
   후보 1명일 때만 미리 선택, 2명 이상이면 나열만 하고 선택하지 않음
5) 같은 nameRaw가 2개 이상 → 그 이름을 쓰는 항목 전부 duplicate_name 승격
6) confidence 파생 (reviewReasons가 비면 high, 아니면 needs_review)

## 지켜야 할 것

설계 원칙: AI는 발췌만, 계산·대조·판단은 코드가 합니다.
이 원칙을 깨는 제안은 하지 말아주세요.
("AI에게 날짜를 계산시키면 코드가 준다" 같은 제안이 나올 수 있는데,
 그 원칙이 이 프로젝트의 차별점입니다.)

DB는 1차에서 넣지 않기로 확정했습니다.
다만 나중에 붙일 수 있게 응답 구조만 잡아주세요
(항목마다 안정적인 id, 회의 단위 메타데이터 분리 정도).

## 확인 방법

05번 샘플을 넣었을 때 커피머신 항목이 이렇게 나와야 합니다:

  dueDateRaw: "만료 30일 전"
  anchorDateRaw: "11월 2일"
  → 코드가 계산한 실제 기한: 2026-10-03
  reviewReasons: ["no_assignee"]

여기까지 나오면 후처리가 제대로 붙은 것입니다.
```
