# 함정 정답지 (데모 설명용)

> 이 문서는 데모에서 "여기를 이렇게 처리했습니다"라고 짚기 위한 것입니다.
> AI에게 주는 입력에는 포함하지 않습니다.
> 스키마(`extract-prompt.md`)와 짝을 이룹니다. 스키마가 바뀌면 기대동작도 같이 고칩니다.
>
> 기준 스키마: **3차** (안건 계층 추가, 섹션 4.5)
>
> **만점이 31 → 41로 바뀌었습니다.** 샘플마다 «안건 수»·«안건 묶음» 두 줄을
> 새로 넣었습니다. 아래 「왜 만점을 다시 짰나」를 먼저 읽으십시오.
> 5차까지의 점수(29/31)와 6차 이후 점수는 **척도가 달라 직접 비교되지 않습니다.**

## 01_주간업무회의_0908  (회의일 2026-09-08 화)

| 함정 | 위치 | 기대 동작 |
|---|---|---|
| **안건 수** | — | **4개.** 채용 서류 검토 / 법인카드 정산 / 정수기 계약 / 근태 시스템 오류 |
| **안건 묶음** | 정수기 | 정수기 관련 발화 3개(만료·고장·견적)가 **한 안건 안**에. 결정·미결로 갈라 딴 안건에 두면 실패 |
| 이름 표기 흔들림 | 김민수 / 김 대리 / 민수씨 → 동일인 | `assignee_unmatched` → **되묻기**. 참석자 중 "김"씨가 김민수 1명뿐이므로 **후보로 미리 제안**, 클릭 한 번으로 확정 |
| 상대 날짜 | "이번 주 안에", "담주 화요일" | 9/8 기준 변환 (담주 화요일 → **9/15**) |
| 결정 vs 미결 | 정수기 건 → 견적만 받기로, 결론 없음 | 계약 연장/교체는 `open`, 견적 받기는 `action` + `no_assignee` |
| 잡담 | 국밥집 | 제외 |
| 완료된 항목 | 노트북 발주 (이미 완료) | 신규 액션아이템으로 잡지 않음 |
| 액션 아닌 일정 | "다음 회의 9/15" | 액션아이템 아님 |
| **발화자 ≠ 담당자** | "민수씨가 **말한** 근태 오류 건" | 민수씨를 담당자로 넣지 않음 → `no_assignee` |
| 담당자 명확 | 영수증 연락 → "정다은 님이" | 정상 추출, `reviewReasons: []` (대조군) |

## 02_외주계약_논의_0910  ★난이도 높음

| 함정 | 기대 동작 |
|---|---|
| **안건 수** | **1개.** 회의 전체가 "홈페이지 리뉴얼 외주" 한 화제입니다. 견적 비교·번복·계약서 초안·착수 시점이 전부 같은 안건입니다 |
| **안건 묶음** | 번복된 결정과 그 뒤의 "확인해보고 결정"이 **같은 안건**에 있어야 합니다. 갈리면 취소선 항목이 맥락 없이 홀로 남습니다 |
| **동명이인** | `attendeesRaw`에 `{김민수, 회계}` `{김민수, 개발팀}` 둘 다 유지. 합치지 않음. 같은 `nameRaw` 2개 → `duplicate_name` → **되묻기**(후보 2개, 자동 선택 안 함) |
| **번복** | 최종 `type: "open"`. 뒤집힌 문장은 `supersededQuote: "일단 A사로 가는 걸로 정함."` + `superseded` → 화면에 **취소선**으로 남김 |
| 담당자 미지정 | "다른 팀 담당자에게 확인" → `assigneeRaw: null` + `no_assignee` → 되묻기 |
| 단위 모호 | "3천", "4천2백" → `unit_unclear` → 되묻기. 추측 금지 |
| 조건부 | "업체 확정 후 계약서 검토" → `blockedByRaw: "업체 확정된 뒤에"` + `blocked`. **`dueDateRaw`에 넣지 않음** |
| 상대 기한 | "이번 달 안에" → 9/30 |

## 03_전사_정기보고_준비_0911  (회의일 2026-09-11 금)

| 함정 | 기대 동작 |
|---|---|
| **안건 수** | **1개** (임원보고 자료 준비). 2개(자료 양식 / 인력 현황 자료)로 갈라도 오답 아님. **3개 이상으로 쪼개지면 실패** — 양식 통일·목차·취합 마감은 한 화제입니다 |
| **안건 묶음** | 철회된 회의실 예약 시스템 건으로 **안건을 만들지 않습니다.** 2단계에서 버린 화제는 안건도 아닙니다 |
| **참석자 미기재** | "팀장님이랑 둘이" — `attendeesRaw`에 팀장님만. 화자는 본문에 없음 |
| 1인칭 담당자 | "양식 만드는 건 내가" → `assigneeRaw: "내가"` + `assignee_unknown` → 되묻기 |
| 기한 아닌 선행조건 | 인사팀 요청 → `blockedByRaw: "거기 담당자가 휴가라 돌아오면"` + `blocked`. **`dueDateRaw`로 새지 않음** |
| 취소된 안건 | 회의실 예약 시스템 → 제외 |
| 요일 표현 | "다음 주 수요일" → **9/16**, "화요일 오전" → **9/15** |

## 04_회의록_전사본_0912  ★안건 가르기 기준 샘플

| 함정 | 기대 동작 |
|---|---|
| **안건 수** | **3개.** 사내 문의 대응 / 경비 정산 마감일 / 다음 달 워크샵 |
| **안건 묶음** | FAQ 게시 위치 미결("노션이 접근이 안 되는 분들도")이 **사내 문의 대응 안건 안**에 있어야 합니다. 떼어내면 "무슨 게시 위치인지" 알 수 없습니다 |
| 형식 자체가 다름 | 타임스탬프 대화체. 같은 스키마로 뽑혀야 함 |
| **기한이 기한이 아님** | "여유 될 때", "다음 회의 전까지" → `due_unparseable` |
| 미결 | FAQ 게시 위치 → `open` |
| 잡담 구간 | 14:18 주차장 | 제외 |
| **자발적 담당자** | 이수현이 **한 발화 안에서** 스스로 맡음 ("제가 재무팀 이번 주에 볼 일 있어서 그때 물어볼게요") → 담당자 = 이수현. 정상 추출 |
| **2턴 걸친 지시 확인** | "뽑아주세요" → "제가요?" → "네 수현 씨가" → **담당자를 비우고 `no_assignee` → 되묻기** |

> **04번 마지막 항목은 "못 잡는 것"이 아니라 설계 결정입니다.**
> 여러 발화를 이어 붙여 담당자를 추론하려면 `quote`가 배열이어야 하고,
> 그러면 환각 탐지(인용문이 원문에 있는가)가 배열 순회로 복잡해집니다.
> 추론해서 채우면 틀려도 티가 안 나지만, 비워두면 화면에 드러납니다.
> 데모에서는 **한계를 감추지 않은 지점**으로 설명합니다.

---

## 왜 만점을 다시 짰나 (31 → 41)

**04번을 옛 정답지로 채점하면 거의 만점입니다.** 자발적 담당자(이수현) ✓,
2턴 지시 → `no_assignee` ✓, "여유 될 때"·"다음 회의 전까지" → `due_unparseable` ✓,
14:18 잡담 제외 ✓.

그런데 나온 결과물은 쓸 수 없었습니다. 실제 안건 3개가 항목 7개로 흩어져서,
"정다은 씨가 한 달치 질문 뽑아서 분류해주시고"라는 항목만 보면 **무슨 질문인지**
알 수 없었습니다. 답은 세 발화 앞에 있는데 그 발화는 다른 칸에 있었습니다.

**점수는 맞는데 쓸모가 없었습니다.** 정답지가 "무엇을 하나의 안건으로 볼
것인가"를 아예 재지 않아서, 29/31로도 이 구멍이 안 보였습니다.
측정 지표가 제품 품질과 어긋나 있었고, 지표를 고치기 전까지는 고쳐도 티가
나지 않았을 것입니다.

그래서 샘플마다 «안건 수»와 «안건 묶음» 두 줄을 넣었습니다.
10점이 늘어 만점 41점(01: 10 / 02: 8 / 03: 7 / 04: 8 / 05: 8)입니다.

### 채점할 때 주의

- «안건 수»는 **정확히 일치**할 때만 1점입니다. 03번만 허용 범위를 적어 뒀습니다.
- «안건 묶음»은 그 줄에 적힌 **한 가지**만 봅니다. 안건 전체가 완벽한지 재는
  줄이 아닙니다 — 그러면 사람마다 점수가 갈립니다.
- 안건 **제목과 요약의 품질은 채점하지 않습니다.** 대조할 원문이 없어 옳고
  그름을 가릴 기준이 없습니다. 화면이 "확인이 필요합니다"를 붙이는 것으로
  갈음하고, 대신 **원문에 없는 숫자·이름·날짜가 요약에 있으면 그 샘플의
  «안건 묶음» 점수를 0점 처리**합니다. 이건 가릴 수 있는 종류입니다.

## 05_비품_예산_0913  (회의일 2026-09-13 일)

| 함정 | 기대 동작 |
|---|---|
| **안건 수** | **6개.** 원문이 이미 1~6번으로 갈라 놓았습니다. 그대로 따라가면 됩니다 |
| **안건 묶음** | 1번의 "토너 과다 사용 확인 필요"가 **토너 안건 안**에 있어야 합니다(원칙 8). 새 안건으로 떼면 실패. 반대로 6개를 2~3개로 뭉치는 것도 실패 |
| **조건부 결정** | 차액 월 10만원 이내면 변경 / 초과면 유지 → `conditional` 배지. 단순 결정으로 뭉개지 않음 |
| 보류 항목 | 시상식 기념품 → 예산 미확정 → `open`, 액션 아님 |
| **날짜 있는 리스크** | 커피머신 자동갱신. `dueDateRaw: "만료 30일 전"` + `anchorDateRaw: "11월 2일"` → 코드가 **10/3** 계산 |
| 부수 의견 | 토너 과다 사용 "확인 필요" → `action` + `no_assignee` |
| 애매한 발화 | "우산꽂이 하나 사자" → `ambiguous_intent` → 노란 배지 |
| 상대 날짜 | "다음 주 수요일" 반차 → **9/16** |

---

## 데모에서 강조할 3가지

1. **번복 처리** (02) — 마지막 상태를 읽고, 뒤집힌 문장을 `supersededQuote`로 남겨 취소선 표시
2. **되묻기** (02, 03, 04) — 담당자·단위·인물이 불확실할 때 추측하지 않고 사람에게 물음
3. **역산** (05) — `anchorDateRaw` 하나로 만료일에서 통보기한을 계산 (11/2 → 10/3)

셋 다 "그냥 요약"으로는 안 되는 항목입니다.

## 같은 규칙, 반대 동작 (데모 포인트)

참석자 이름 후보 제안은 **상황에 따라 반대로 작동합니다.**

| 상황 | 동작 |
|---|---|
| 01 — "김 대리", 참석자 중 김씨 **1명** | 김민수를 후보로 **미리 선택**해 띄움. 편의 |
| 02 — "김민수", 참석자 중 김민수 **2명** | 후보 2개를 띄우고 **아무것도 선택하지 않음**. 안전장치 |

코드는 같고, 유일할 때만 제안합니다. 애매하면 사람이 고릅니다.

---

## 스키마 변경 시 같이 고칠 것

- 새 `reviewReasons` enum을 추가하면 → 해당 샘플의 기대동작에 반영
- 필드를 추가하면 → 그 필드를 쓰는 샘플 표에 필드명 표기
- 설계상 "안 하기로 한 것"이 생기면 → 04번처럼 **인용구로 이유를 남김**
- **함정을 추가·삭제하면 → 만점과 `docs/regression.md`의 척도 표기를 같이 고침.**
  만점이 바뀌면 이전 회차 점수와 직접 비교가 안 된다는 것도 같이 적을 것
